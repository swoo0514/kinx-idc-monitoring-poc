# Ansible이 다룰 "대상" = Zabbix API (서버 SSH 아님). httpapi 연결로 API 호출.
[zabbix]
zabbix_lab ansible_host=127.0.0.1

# 3종 에이전트 배포 대상 = 실제 호스트(SSH). deploy_agents.yml 이 사용.
# agent_identity = 세 시스템 공통 식별자(FQDN). 이 값으로 정규화되어 매핑 불필요.
#
# 호스트명은 FQDN 으로 적는다. 데모 B 에서 조치 대상은 게이트웨이가 Zabbix 알림에서 받은
# 이름(FQDN)으로 오므로, 인벤토리가 짧은 이름을 쓰면 매칭에 실패한다. 2026-07-29 실측에서
# 인벤토리가 'vm-p3-target-002', 알림이 'vm-p3-target-002.novalocal' 이라 조치가 통째로
# 건너뛰어졌다. 네 시스템(Zabbix·Loki·Wazuh·Ansible)이 한 이름을 쓰는 것이 전제다.
[targets]
# 예: db-target-001.novalocal ansible_host=192.0.2.XX agent_identity=db-target-001.novalocal

[targets:vars]
ansible_user=rocky
# ansible_ssh_private_key_file=~/.ssh/... (필요 시)

[zabbix:vars]
ansible_connection=httpapi
ansible_network_os=community.zabbix.zabbix
ansible_httpapi_port=8080
ansible_httpapi_use_ssl=false
ansible_httpapi_validate_certs=false
ansible_zabbix_url_path=''
