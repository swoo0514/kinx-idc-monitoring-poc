# 알림 게이트웨이 기술 명세서 (데모 B·C 공용)

## 1. 시스템 개요 및 위치

본 모듈은 사내 및 MSP Zabbix, Wazuh 관측 시스템의 알림 데이터를 단일 접점에서 수집하여 **통합 심각도 정규화(SEV) ➔ 멱등성 검증 ➔ 태그 기반 라우팅**을 전담하는 공용 알림 게이트웨이(Egress Gateway)입니다. AI 기반 초동 분석(데모 C) 및 승인 기반 자가 치유 파이프라인(데모 B)의 공통 백엔드로 동작합니다.

```text
Zabbix Webhook Media Type ─┐
                           ├─▶ [통합 게이트웨이] 토큰 검증 ➔ 멱등성 검사 ➔ SEV 정규화 ➔ 태그 라우팅
Wazuh Integrator ──────────┘                                            │
                                 ┌──────────────────────────────────────┴──────────────────────────────────────┐
                                 ▼                                      ▼                                      ▼
                      triage (데모 C 트리아지)               remediate (데모 B 자가 치유)                digest / dashboard_only
```

---

## 2. 파일 구조 및 구성 모듈

| 파일 경로 | 주요 역할 및 담당 기능 |
|---|---|
| `gateway/app.py` | FastAPI 웹 애플리케이션 — REST 엔드포인트 제공, 토큰 검증, 멱등성 처리, 비동기 디스패치 |
| `gateway/severity.py` | 통합 심각도 정규화 상수 정의 — [`severity-normalization.md`](../docs/02-design/severity-normalization.md)의 코드 구현체 |
| `gateway/router.py` | 태그 기반 라우팅 로직 — `automate`, `scope` 태그 조합을 통한 처리 경로 분기 |
| `gateway/selftest.py` | 파이프라인 순수 제어 로직 독립 검증 모듈 (FastAPI 의존성 없이 실행) |
| `gateway/collector.py` | Zabbix, Loki, Wazuh 텔레메트리 데이터 비동기 교차 수집기 |
| `gateway/prejudge.py` | 과거 90일 발생 이력 기반 결정론적 만성/신규 장애 선판정 모듈 |
| `gateway/incident.py` | (감시 영역, 호스트, 알림 유형) 기반 디바운스 창 제어 및 알림 병합(Incident Merging) 모듈 |
| `gateway/llm.py` | Claude/Ollama LLM 어댑터 및 양방향 가명화(Masking) 파이프라인 |
| `gateway/slack.py` | Slack Block Kit 기반 알림 카드 및 트리아지 결과 회신 모듈 |
| `gateway/triage.py` | 수집 ➔ 가명화 ➔ LLM 연산 ➔ Slack 회신 전체 파이프라인 오케스트레이터 |
| `gateway/masking.py` | 호스트명·IP·그룹명 양방향 가명화 및 전송 항목 화이트리스트 |
| `gateway/keep.py` | 관제 도구(Keep) 알림 등록 — 조치 후보·생략 사건·분석 결과 |
| `gateway/pending.py` | 디바운스 대기 알림 영속화 및 재기동 시 재투입 (§8-4) |
| `gateway/heartbeat.py` | 생존 신호 전송 및 처리량 집계 (§20) |
| `gateway/holmes.py` | HolmesGPT 심층 조사 연동 (§10) |

**Zabbix 서버 측 스크립트는 본 패키지에 두지 않습니다.** 웹훅 미디어 타입 스크립트(`ansible/files/zabbix_media_webhook.js`·`zabbix_media_selfwatch.js`)는 Zabbix 서버 내장 엔진에서 실행되며 게이트웨이 프로세스가 읽거나 실행하지 않습니다. Ansible 이 `lookup('file')` 로 본문을 읽어 API 로 등록하고, 이후에는 Zabbix 서버 DB 에 저장된 사본이 동작합니다. 파이썬 패키지 내에 배치할 경우 게이트웨이 구성 요소로 오인됩니다.

---

## 3. 실행 및 검증 방법

```bash
# bot/ 디렉토리 이동 및 가상환경 설정
cd bot
source ~/bot-venv/bin/activate
pip install -r requirements.txt

# 게이트웨이 인증 토큰 설정 (코드 내 하드코딩 금지)
export GATEWAY_TOKEN="<임의의_긴_랜덤_문자열>"

# 게이트웨이 데몬 기동 (단일 Worker 프로세스로 실행)
python -m uvicorn gateway.app:app --host 0.0.0.0 --port 8800
```

### 독립 검증 명령

```bash
# 순수 로직 단위 테스트 실행 (통과 시 검증 건수와 함께 ALL OK 를 출력)
python -m gateway.selftest

# 헬스체크 엔드포인트 호출 검증
curl http://localhost:8800/healthz
```

---

## 4. REST API 엔드포인트 명세

### 4-1. POST `/webhook/zabbix`

Zabbix 웹훅 액션으로부터 알림 데이터를 수신합니다 (헤더 `X-Gateway-Token` 인증 필수).

**요청 페이로드 예시:**
```json
{
  "source": "zabbix-internal",
  "event_id": "12345",
  "event_value": 1,
  "event_name": "Filesystem /data usage is high on lab-web01",
  "nseverity": 4,
  "host": "lab-web01.novalocal",
  "tags": [
    {"tag": "automate", "value": "service_restart"},
    {"tag": "scope", "value": "full"}
  ]
}
```

**응답 페이로드 예시 (신규 수신):**
```json
{
  "status": "accepted",
  "sev": "SEV2",
  "route": "remediate",
  "playbook": "service_restart"
}
```

**응답 페이로드 예시 (중복 수신):**
```json
{
  "status": "duplicate"
}
```
*(동일 `(source, event_id, event_value)` 조합 수신 시 HTTP 200 `duplicate`를 반환하여 Zabbix의 불필요한 재전송을 즉시 중단시킵니다.)*

### 4-2. POST `/webhook/wazuh`

Wazuh Manager 커스텀 연동 스크립트로부터 경보 데이터를 수신합니다. (`alert_id`, `rule_id`, `rule_level`, `agent_name` 전달)

---

## 5. Zabbix 미디어 타입 배선 명세

Zabbix Administration ➔ Media types ➔ Create Media Type: Type = **Webhook**, Script = `ansible/files/zabbix_media_webhook.js`

스크립트 본문은 Ansible 이 `lookup('file')` 로 읽어 API 로 등록하며, 이후 Zabbix 서버 DB(`media_type.script`)에 저장되어 **Zabbix 서버 내장 엔진에서 실행**됩니다. 게이트웨이 프로세스는 본 스크립트를 읽거나 실행하지 않습니다.

### 설정 파라미터 매핑표

| 파라미터 명칭 | 설정값 (Zabbix 매크로) | 검증 상태 및 비고 |
|---|---|---|
| `gateway_url` | `http://<게이트웨이_IP>:8800` | 환경별 IP 지정 |
| `token` | `{$GATEWAY_TOKEN}` | DB 저장 파라미터 |
| `source` | `zabbix-internal` 또는 `zabbix-msp` | 소스 구별 매크로 |
| `event_id` | `{EVENT.ID}` | 공식 매크로 검증 완료 |
| `nseverity` | `{EVENT.NSEVERITY}` | Numeric event severity 매크로 검증 완료 |
| `event_name` | `{EVENT.NAME}` | 공식 매크로 검증 완료 |
| `tags_json` | `{EVENT.TAGSJSON}` | JSON Array event tags 매크로 검증 완료 |
| `host` | `{HOST.HOST}` | FQDN 호스트명 매크로 검증 완료 |
| `event_value` | `{EVENT.VALUE}` | 장애(1)/해소(0) 구분 매크로 |

---

## 6. 핵심 아키텍처 의사결정 및 기술적 근거

1. **`severity.py` 단일 구현체 적용 (Single Source of Truth):**  
   사내 Zabbix Warning ➔ `SEV4` (노이즈 강등), MSP Warning ➔ `SEV3` (고객 통보 신호 유지), Wazuh 레벨 10+ ➔ `SEV2` 정규화 규칙을 코드 수준에서 엄격히 보장합니다.
2. **미지 소스 및 예외 상태의 페일세이프(Fail-safe) 정규화:**  
   미정의 소스 또는 스펙 범위를 벗어난 데이터 수신 시 과소평가로 인한 장애 누락을 방지하고자 `SEV2`로 상향 정규화합니다.
3. **상수 시간 비교를 통한 타이밍 공격 차단 (`hmac.compare_digest`):**  
   인증 토큰 비교 시 단순 `==` 연산자 대신 `hmac.compare_digest`를 적용하여 타이밍 부채널(Timing Side-channel) 공격 위험을 차단합니다.
4. **인메모리 1시간 TTL 멱등성 버퍼 연산:**  
   동일 알림 재전송 시 중복 LLM API 호출 및 중복 Slack 카드 발송을 차단합니다. (프로덕션 환경 적용 시 Redis/DB 지속화 스토리지로 마이그레이션 추진)
5. **계약 조건의 코드화 (`scope` 우선순위 제어):**  
   트리거에 `automate` 태그가 존재하더라도 위탁 계약 속성이 `scope=notify_only`일 경우, `router` 단에서 자동 조치 경로 진입을 차단하고 `triage` 경로로 강제 라우팅합니다.

---

## 7. 컨텍스트 수집기 및 선판정 로직 (`collector.py` / `prejudge.py`)

트리아지 경로 진입 시 LLM 연산에 필요한 재료 데이터를 3개 소스로부터 비동기 교차 수집하고 결정적 선판정을 수행합니다.

### 7-1. 교차 소스 텔레메트리 수집 (Multi-source Context Collection)

- **Loki (로그 축):** `/loki/api/v1/query_range` 엔드포인트를 통해 `{<라벨>="<host>"}` **정확 일치** 조건으로 최근 15분간의 로그를 최대 300줄 또는 2MB 까지 수집한 뒤(라인당 300자 제한) 반복을 접어 보냅니다(§26). 라벨명은 `LOKI_HOST_LABEL`(기본 `host`)로 지정하며, 호스트명 해석 규칙과 0건 응답의 판정은 §12 를 따릅니다.
- **Wazuh (보안 축):** OpenSearch `wazuh-alerts-*/_search` 엔드포인트를 통해 `agent.name` 조건으로 최근 15분간 발생한 보안 경보 최대 20건을 수집합니다.
- **조회 실패 및 미배선 처리:** API 호출 실패 시 예외를 삼키지 않고 `sources` 메타데이터에 `unavailable` 상태를 기록하여, 거짓 안심(False Confidence) 기반 분석을 차단합니다.

### 7-2. Zabbix API 읽기 전용 5종 병렬 조회

1. `event.get`: 현재 이벤트 상세 및 태그 수집
2. `trigger.get`: 트리거 조건식 및 메트릭 역참조 정보 수집
3. `item.get` ➔ `history.get`: 관련 메트릭 최근 1시간 수치 추이(20개 데이터 포인트) 수집
4. `event.get` (90일 창): 동일 트리거의 과거 누적 발생 이력 조회 (선판정 연산 입력값)
5. `host.get`: 호스트 메타데이터 및 그룹 정보 수집

*①~④ 항목은 `asyncio.gather`를 통해 병렬 호출되며, 개별 콜 타임아웃을 5초로 제한하여 30초 처리 예산 내 구동을 보장합니다.*

### 7-3. 결정적 만성/신규 선판정 규칙 (`prejudge.py`)

과거 90일간 동일 트리거의 누적 발생 횟수를 바탕으로 결정론적 판정을 내리며, LLM이 이를 재판정하거나 변경할 수 없습니다.

| 최근 90일 발생 횟수 | 최종 판정 | LLM 회신 톤 및 출력 메시지 |
|---|---|---|
| 0회 | **신규 (New)** | *"처음 관측된 미지 장애 — 즉시 원인 확인 권장"* |
| 1~2회 | **재발 (Recurrent)** | *"최근 90일 내 N회 재발된 장애 — 이전 발생 건과의 공통점 점검"* |
| 3회 이상 | **만성 (Chronic)** | *"알려진 만성 반복 장애 (90일간 N회 발생) — 정비 대상 항목"* |

#### 만성 기준의 산정 근거

**본 임계값은 횟수가 아니라 재발 간격으로 정의합니다.** 횟수로 고정할 경우 두 가지 문제가 발생합니다. 첫째, 특정 수치를 선택한 근거를 제시할 수 없습니다. 둘째, 관측 창을 90일에서 180일로 변경하면 동일 횟수의 의미가 2배로 완화되나 설정상 변화가 드러나지 않습니다.

| 설정 항목 | 기본값 | 산출 |
|---|---|---|
| `PREJUDGE_WINDOW_DAYS` | 90 | 관측 창 |
| `PREJUDGE_CHRONIC_INTERVAL_DAYS` | 30 | 만성으로 판정할 재발 간격 |
| 만성 하한(산출값) | 3 | `ceil(창 ÷ 간격)`, 최솟값 2 |
| `PREJUDGE_CHRONIC_MIN` | (미설정) | 지정 시 산출값 대신 해당 값을 사용 |

**30일 선정 근거:** MSP 월간 리포트 발행 주기와 일치시킵니다. 월 1회 이상 재발하는 항목은 리포트 발행 시마다 포함되며, 매 리포트에 반복 등장함에도 해소되지 않은 항목이 정비 대상에 해당합니다. 즉 **"월간 리포트에 매회 등장하는 항목"**을 만성으로 정의합니다. 리포트 주기 변경 시 본 값도 함께 조정합니다.

**외부 기준 부재:** SRE 문헌에서 반복 알림의 만성 판정 횟수를 규정한 자료는 확인되지 않았습니다. ITIL 문헌에는 "동일 증상 30일 내 3건 이상 시 별도 관리 대상 등록"이 통상적 관행으로 언급되나, 동일 자료에서 표준이 아닌 조직별 조정값임을 명시하고 있습니다(간격 환산 시 본 기준과 동일한 30일).

**실측 분포 (사내 Zabbix 90일):** 트리거 계열의 71.7%가 관측 기간 중 1회만 발생하며, 상위 2.0%(200회 이상)가 전체 이벤트의 95.0%를 차지합니다. 이러한 분포에서는 임계값을 2회로 낮추거나 50회로 상향하여도 **이벤트 기준 만성 비중은 97~99% 구간에서 변동하지 않습니다.** 임계값 변경이 실질적으로 영향을 미치는 대상은 정비 후보 목록의 범위(계열 기준 10.5% → 18.8%)입니다.

---

## 8. 알림 병합 메커니즘 (`incident.py`)

### 8-1. 알림 병합 및 브리지 규칙 (Incident Merging & Bridge Rules)

단일 알림 단위 트리아지의 한계를 극복하기 위해 동일 호스트 및 연관 유형의 알림들을 디바운스 창(Debounce Window) 동안 수집하여 **단일 인시던트로 병합 처리**합니다.

- **기본 병합 키:** `incident_key(host, class) = (host, bridge_id(class))`
- **연관 브리지 규칙 (`BRIDGE_GROUPS`):**
  - `{replication, cpu_io_pressure}`: DB 복제 지연 알림과 CPU/IO 자원 경합 알림을 단일 사건으로 병합
  - `{disk_space, service_down}`: 디스크 용량 초과 알림과 서비스 중단 알림을 단일 사건으로 병합
  - `auth_security` (보안 경보) 항목은 브리지 그룹에 포함하지 않고 항상 독립 사건으로 격리 처리합니다.

### 8-1-1. 감시 영역 구분 (Monitoring Realm)

**호스트명은 개별 감시 서버 내에서만 고유합니다.** 사내 Zabbix 와 MSP Zabbix 를 동시에 수용하는 구성에서는 서로 다른 물리 장비가 동일한 호스트명을 가질 수 있으며, 호스트명만으로 병합할 경우 **서로 다른 고객사의 알림이 단일 인시던트로 병합**됩니다. 이 경우 단일 Slack 카드·단일 LLM 호출·단일 리포트 항목에 복수 고객사의 데이터가 포함됩니다.

이에 병합 키를 `(감시 영역, 호스트, 브리지 ID)` 로 구성합니다.

| 환경 변수 | 형식 | 기본값 |
|---|---|---|
| `INCIDENT_REALM_MAP` | `소스=영역,소스=영역` | (미설정 — 전 소스 단일 영역) |

**소스가 아닌 영역 단위로 구분하는 이유:** 동일 장비를 Zabbix 와 Wazuh 가 각각 보고하므로, 소스를 그대로 키에 포함하면 두 관측 소스가 분리되어 교차 소스 병합이 성립하지 않습니다. 영역은 소스의 상위 개념으로, 사내 Zabbix 와 사내 Wazuh 는 동일 영역, MSP Zabbix 는 별도 영역으로 분류합니다.

```bash
INCIDENT_REALM_MAP=zabbix-internal=internal,zabbix-msp=msp,wazuh=internal
```

**미설정 시 동작:** 소스명을 영역으로 사용합니다. 사내 Zabbix 와 MSP Zabbix 는 소스가 상이하므로 **설정 없이도 자동 분리**됩니다. 본 매핑은 분리 목적이 아니라 **통합 목적**입니다 — 동일 장비를 Zabbix 와 Wazuh 가 각각 보고하는 경우 두 소스를 동일 영역으로 지정해야 교차 소스 병합이 성립합니다.

**인시던트 지문에도 영역이 포함됩니다.** 미포함 시 관제 도구(Keep)에서 서로 다른 고객사의 동일 유형 사건이 단일 레코드로 중복 제거되며, 심층 조사 결과가 타 고객사 레코드에 병합될 수 있습니다.

**조회 대상 서버:** 컨텍스트 수집 시 알림이 수신된 감시 서버에 조회합니다. 대상 선택은 호스트 명부의 감시 서버 절이 담당합니다(§8-1-2).

### 8-1-2. 호스트 명부 (Host Registry)

호스트별 속성을 단일 파일로 관리합니다. 도입 이전에는 동일 성격의 정보가 환경 변수 4종(`HOST_LABEL_MAP`·`LOGS_EXEMPT_HOSTS`·`SECURITY_EXEMPT_HOSTS`·`INCIDENT_REALM_MAP`)에 각기 다른 형식으로 분산되어, 호스트 추가 시 4개소를 동시에 수정해야 했습니다.

**명부는 식별자가 아니라 속성을 담습니다.** Zabbix 는 호스트명의 서버 내 유일성을 강제하므로(공식 문서), `(감시 서버, 호스트명)` 조합이 이미 유일한 식별자입니다. 따라서 **명부에 등재되지 않은 호스트도 정상 동작**하며, 명부는 조회 경로와 축 보유 여부를 보완하는 역할입니다.

```yaml
hosts:
  - name: node1                        # Zabbix 호스트명
    source: zabbix-internal            # 생략 시 전 소스에 적용
    realm: internal                    # 감시 영역 (§8-1-1)
    loki: vm-target-001.novalocal      # Loki 라벨값
    wazuh: vm-target-001.novalocal     # Wazuh agent.name
    logs: true                         # 로그 축 보유 여부
    security: true                     # 보안 축 보유 여부
    id: ""                             # 예약 (아래 참조)

sources:                               # 감시 서버가 2대 이상일 때만 필요 (아래 참조)
  - name: zabbix-internal
    realm: internal
    url: http://10.0.0.1:8080
    token_env: ZABBIX_TOKEN
```

| 항목 | 설명 |
|---|---|
| 설정 | `HOST_REGISTRY_FILE` — 미지정 시 기존 환경 변수로 동작 |
| 우선순위 | 명부 → 환경 변수 → 기본값 |
| 매칭 | `(source, name)` 완전 일치 우선, 미존재 시 `source` 미지정 항목 |
| 초안 생성 | `python3 probe.py registry > hosts.yml` — Zabbix·Loki·Wazuh 실 조회 결과로 생성 |
| 로드 실패 | 오류 로그 기록 후 환경 변수 경로로 동작(무음 실패 방지) |

#### 감시 서버 절 (`sources`)

감시 서버가 2대 이상인 환경에서는 **알림이 수신된 서버에 조회해야 합니다.** 사내 알림의 이력을 MSP 서버에 조회할 경우, 해당 호스트가 부재하여 빈 결과가 반환되거나 동일 호스트명을 가진 타 고객사 데이터가 반환됩니다.

```yaml
sources:
  - name: zabbix-internal        # 미디어 타입의 source 파라미터 값
    realm: internal              # 이 서버의 호스트가 속하는 영역
    url: http://10.0.0.1:8080
    token_env: ZABBIX_TOKEN      # 토큰 값이 아니라 **환경 변수명**
  - name: zabbix-msp
    realm: msp
    url: http://10.0.0.2:8080
    token_env: ZABBIX_TOKEN_MSP
```

**토큰을 파일에 기재하지 않는 이유:** 명부는 형상 관리 대상이며 자격 증명은 형상 관리에 포함되어서는 안 됩니다. 파일에는 참조할 환경 변수명만 기재하고 값은 실행 환경에서 조회합니다. 해당 환경 변수가 비어 있을 경우 조회 실패 시점이 아니라 **클라이언트 생성 시점에 오류를 기록**합니다.

**영역 상속:** 감시 서버 절에 `realm` 을 지정하면 해당 서버의 전 호스트에 적용되므로, 호스트별 개별 지정이 불필요합니다. 우선순위는 호스트 항목 → 감시 서버 절 → `INCIDENT_REALM_MAP` → 소스명 순입니다.

**미지정 시 동작:** 기존과 동일하게 `ZABBIX_URL`·`ZABBIX_TOKEN` 단일 인스턴스를 조회합니다.

생존 신호의 발행 측 이벤트 수 조회도 이 절을 사용하여 감시 서버별로 산출합니다(§20-7).

**`id` 필드 미사용 사유:** 호스트명 변경 시에도 이력을 연속 유지하기 위한 대체 키이나, 이를 활용하려면 게이트웨이가 기록을 축적하는 저장소가 선행되어야 합니다(§2-1-1). 현 시점에서는 참조하는 코드가 없으므로 형식만 정의하고 비워 둡니다.

**pyyaml 의존성:** 명부 파싱에만 사용하며, 미설치 시 명부 기능만 비활성화되고 나머지 기능은 정상 동작합니다.

### 8-2. 디바운스 창 제어 (Debounce Window Control)

- **일반 디바운스 대기:** 마지막 알림 수신 후 `INCIDENT_DEBOUNCE_S`(기본 90초) 동안 추가 알림이 없을 경우 인시던트 마감
- **최대 창 허용 시간:** 알림이 지속 수신되더라도 `INCIDENT_MAX_WINDOW_S`(기본 300초) 도달 시 강제 마감 및 트리아지 실행
- **우선순위 단축 대기:** `SEV1` 알림 포함 시 `INCIDENT_PRIORITY_DEBOUNCE_S`(기본 15초)로 대기 시간 단축

### 8-3. 분석 발동 조건 (`should_triage`)

인시던트 마감 후 LLM 분석을 실제로 수행할지 판정합니다. 아래 순서로 평가하며 먼저 성립하는 조건에서 판정을 확정합니다.

| 순서 | 조건 | 판정 | 근거 |
|---|---|---|---|
| 1 | `SEV1` 포함 | 발동 | 위중 사건은 조건 없이 분석 |
| 2 | 복수 알림 병합 | 발동 | 교차 축이 이미 존재 |
| 3 | 교차 소스 조회 실패(`unavailable`) 또는 이름 불일치(`unmatched`) | 발동 | 신호 부재가 아니라 미상이므로 보수적 판단 (§12) |
| 4 | 교차 신호 `INCIDENT_GATE_MIN_CROSS` 건 이상 | 발동 | 단일 알림에 로그·보안 신호가 동반 |
| 5 | 선판정 결과가 **신규** | 발동 | 아래 단일 소스 환경 항목 참조 |
| 6 | 그 외 | 스킵 | 정상 조회 상태에서 교차 신호가 없는 만성·재발 알림 |

조건 3 과 5 에는 각각 독립적인 시간당 상한을 적용합니다(아래 폭주 방지 항목).

**단일 관측 소스 환경 대응:** 로그·보안 축이 미배선된 환경에서는 위 조건 중 3·4 가 성립하지 않습니다. 미배선(`disabled`)은 조회 실패로 계상되지 않고, 교차 신호는 항상 0 으로 산출되며, 90일 상관 분석에서 유효한 교차 유형 조합이 확인되지 않아 병합 조건도 발생하지 않습니다. 잔여 조건인 `SEV1` 은 사내 심각도 분포상 극소수에 해당하므로(Warning 등급 99.5%), 조건 5 가 없으면 분석 구동 빈도가 사실상 0 에 수렴합니다.

이에 따라 **선판정 결과가 신규(최근 90일 내 동일 트리거 이력 없음)인 사건**을 독립 발동 사유로 추가하였습니다. 처음 관측되는 문제는 대응 이력이 존재하지 않으므로 분석의 정보 이득이 가장 큰 구간입니다.

**부하 보호의 위치 (2026-08-11 구조 변경):** 게이트는 **분석 가치 판단만 수행**하며 부하 보호는 담당하지 않습니다. 보호는 호출이 실제 발생하는 지점(`llm.triage_reply`, §20-8)에서 동시 호출 수와 시간당 총량으로 수행합니다.

**변경 전 구조와 그 문제점:** 조건 3·5 에 각각 사유별 시간당 한도를 적용하고 초과 시 발동을 차단하는 방식이었습니다. 세 가지 결함이 있었습니다.

1. **보호 범위가 규칙 목록에 종속됨** — 한도가 규칙 분기마다 개별 배치되어, 규칙 추가 시 한도 적용 여부를 매번 판단해야 했습니다. 누락 시 별도 신호가 없으며, 실제로 발동 경로 5종 중 3종(SEV1·병합·교차 신호)에 미적용 상태였습니다. 다수 호스트 동시 장애 시 실제 발생하는 것은 병합 인시던트이므로, 폭주 시점에 동작하지 않는 보호였습니다.
2. **차단이 곧 폐기** — 한도 초과분은 재시도 없이 폐기되며, 기준은 시간당 도착 순서입니다. 사건의 중요도와 무관하게 늦게 도착한 건이 분석 대상에서 제외됩니다.
3. **기준값의 산정 근거 부재** — 30·20 의 근거가 문서·코드 어디에도 없었습니다.

**변경 후:** 사유별 수치는 유지하되 **통제가 아닌 관측 지표**로 전환하였습니다. 게이트는 발동 횟수와 무관하게 동일하게 판단하며, 사유별 최근 1시간 발동 수를 생존 신호로 전송합니다(`gateway.fire_degraded_1h`·`gateway.fire_new_1h`). 조회 실패 사유가 급증하는 상태는 관측 소스 자체의 이상을 의미하므로 차단이 아니라 통지 대상입니다. 차단으로 처리할 경우 원인이 은폐됩니다.

조건 5 전체를 해제하려면 `INCIDENT_GATE_FIRE_ON_NEW=0` 을 설정합니다.

### 8-4. 대기 알림 영속화 (Pending Alert Persistence)

디바운스 창 대기 중 프로세스가 종료될 경우 해당 인시던트가 소실되며, 발송 측은 이미 성공 응답(200)을 수신하였으므로 재전송을 수행하지 않습니다. 이를 방지하기 위해 대기 상태 알림을 파일에 선기록한 후 응답합니다.

| 항목 | 내용 |
|---|---|
| 기록 대상 | `triage` 경로 알림 (디바운스 대기가 발생하는 유일한 경로) |
| 기록 위치 | `GATEWAY_PENDING_FILE` (기본 `~/.kinx-gateway/pending.jsonl`) |
| 기록 방식 | JSON Lines 추가 기록 후 `fsync` 완료 시점에 200 반환 |
| 기록 실패 시 | HTTP 503 반환 → Zabbix 미디어 타입 `Attempts`(기본 3회) 재시도로 위임 |
| 제거 시점 | 인시던트 분석 완료 후 |
| 재기동 처리 | 잔여 항목 자동 재투입, 시도 한도 `GATEWAY_PENDING_MAX_REPLAY`(기본 3회) 초과 시 폐기 및 오류 로그 기록 |

**제거 시점 선정 근거:** 분석 수행 전 제거 시 분석 구간 장애에서 알림이 소실되며, 분석 완료 후 제거 시 동일 상황에서 재기동 후 카드가 1회 중복 게시됩니다. 중복 게시는 육안 식별이 가능하나 소실은 식별이 불가능하므로 후자를 채택하였습니다.

**적용 범위 한계:** `digest`·`dashboard_only`·`remediate` 경로는 대기 없이 즉시 처리되므로 기록 대상에서 제외하였습니다. 해당 경로에서도 처리 직전 프로세스 종료 시 소실이 발생할 수 있으나, 대기 구간(90~300초) 대비 노출 시간이 현저히 짧습니다.

---

### 8-5. 수동 분석 요청 (Manual Analysis Request)

발동 조건(§8-3)에 의해 분석이 생략된 사건에 대해, 관제 담당자가 임의 시점에 분석을 직접 요청할 수 있는 경로를 제공합니다. 관측 소스 미배선 환경에서는 생략 비율이 높으므로, 해당 경로가 없을 경우 봇의 판정이 최종 결정으로 확정되며 사람이 이를 번복할 수단이 존재하지 않습니다.

| 항목 | 내용 |
|---|---|
| 진입점 | Keep 알림 카드의 Run Workflow (`keep/workflows/analyze_now.yml`) |
| 안전 게이트 | `alert.playbook == 'analyze'` — 해당 속성이 없는 카드에서는 미동작 |
| 대상 카드 | 발동 조건 미충족으로 생략된 사건, `digest`·`dashboard_only` 경로 알림 |
| 실행 모듈 | `bot/tools/analyze_now.py` (SSH 경유 실행) |
| 발동 조건 | `run_incident(force=True)` — 조건 재평가를 수행하지 않음 |
| 결과 출력 | 기존 트리아지와 동일한 Slack 카드 |

**사건 복원 방식:** 생략 시점의 컨텍스트를 저장하지 않고, 알림 식별자 문자열(`analyze_ref`)만 카드에 실어 요청 시점에 Zabbix 를 재조회합니다. 이는 생략 이후 경과 시간 동안 변화한 상태를 분석에 반영하기 위함입니다. 형식은 `소스,이벤트ID,트리거ID,유형` 이며 복수 알림은 `|` 로 연결합니다.

**발동 조건 미적용 근거:** 본 경로는 봇의 판정을 사람이 번복하는 절차이므로, 조건을 재평가할 경우 요청 자체가 무시됩니다.

**랩 검증 (2026-08-10):** 발동 조건에 의해 생략된 사건을 대상으로 실행하여 수집·분석·Slack 게시 전 구간 정상 동작을 확인함(총 20.8초, Claude 정상 경로).

### 8-6. 정상 종료 시 대기 사건 마감 (Graceful Flush)

프로세스 종료 시그널 수신 시 디바운스 대기 중인 사건을 즉시 마감하고 처리합니다. §8-4 의 대기 알림 영속화가 강제 종료를 대상으로 한다면, 본 항목은 정상 종료 경로에 해당합니다.

**미적용 시 영향:** 데이터는 대기 파일이 보존하므로 소실되지 않으나, 두 가지 손실이 발생합니다.

| 항목 | 내용 |
|---|---|
| 대기 시간 재계산 | 재기동 후 디바운스 창을 처음부터 다시 계산함. 기존 대기분이 무효화됨 |
| 재투입 횟수 소모 | 재기동 1회당 재투입 시도 횟수가 1 증가함. 배포 등으로 반복 재기동 시 미처리 알림이 상한(`GATEWAY_PENDING_MAX_REPLAY`, 기본 3회)에 도달하여 폐기됨 |

**제한 시간:** `INCIDENT_FLUSH_TIMEOUT_S`(기본 25초). 초과분은 대기 파일에 잔존하여 재기동 후 처리됩니다. 종료가 무한정 지연되면 관리자가 강제 종료하게 되며, 그 경우 정상 종료 경로 자체가 성립하지 않습니다.

**대기 목록 제거 시점:** 처리 완료 이후로 한정합니다. `finally` 절에 배치할 경우 마감 제한 시간 초과에 따른 취소 시에도 제거가 실행되어, 처리하지 않은 알림이 소실됩니다.

---

---

## 9. 실시간 원시 알림 표출 (Fast-Path Pipeline)

디바운스 창 마감 대기 시간으로 인한 초동 통보 지연을 방지하기 위해 **원시 알림 Fast-Path 파이프라인**을 제공합니다.

```text
알림 수신 ──▶ [0초] 원시 알림 카드 즉시 게시 (Slack 최상위 메시지)
   │
   ├─▶ 후속 알림 수신 ──▶ 동일 Slack 스레드 답글로 추가 표출
   │
   └─▶ 디바운스 창 마감 ──▶ LLM 초동 분석 결과를 동일 스레드 답글로 최종 회신
```

인시던트 버퍼 생성 시 최상위 Slack 메시지를 즉시 생성하고, 이후 수집되는 알림 및 최종 LLM 분석 결과를 해당 메시지의 스레드(Thread) 답글로 병합하여 메시지 파편화를 방지합니다.

---

## 10. HolmesGPT 심층 조사 연동 (`holmes.py`)

선판정 결과를 활용하여 분석 가치가 높은 미지 장애 및 긴급 장애에 심층 조사 리소스를 효율적으로 배분합니다.

### 심층 조사 발동 순서 및 조건

1. `scope=notify_only` 및 마스킹 미배치 조건 ➔ **발동 차단** (보안 유출 방지)
2. `SEV1` 긴급 장애 ➔ **무조건 발동**
3. 게이트웨이 1차 분석 열화(`degraded`) 상태 ➔ **무조건 발동** (분석 품질 보완)
4. 선판정 결과 **만성 (Chronic)** 장애 ➔ **억제** (기지 장애에 대한 리소스 남용 방지)
5. 선판정 결과 **신규 (New)** 장애 ➔ **우선 발동** (정보 이득 극대화)
6. 복수 알림 병합 사건 ➔ **발동**

심층 조사 결과는 신규 알림 카드를 생성하지 않고, 1차 분석 결과가 게시된 **Slack 스레드의 답글 및 Keep 인시던트 Note 항목**에 비동기(`asyncio.create_task`)로 첨부됩니다.

---

## 11. 자가 치유 조치 후보 경로 연동 (`router.py` / `keep.py`)

트리거 내 `automate` 태그가 정의되고 계약 제약이 없는 경우, 게이트웨이는 알림을 `remediate` 경로로 라우팅하고 Keep 승인 큐에 등록합니다.

```text
Zabbix 트리거 (automate=service_restart)
  │
  ▼
게이트웨이 라우팅 (route=remediate) ──▶ Keep 승인 큐 등록 (상태: 승인 대기)
                                               │
                                      [관제 담당자 승인 클릭]
                                               │
                                               ▼
                                   Keep 워크플로 실행 ➔ Ansible 플레이북 실행 ➔ 상태 재검증
```

### Keep 워크플로 동적 매크로 연동

Ansible 실행 파라미터 하드코딩을 제거하고, Keep 알림 객체의 동적 필드를 매핑하여 플레이북을 호출합니다:
- `target_host`: `{{ alert.host }}`
- `service_name`: `{{ alert.service }}`
- `playbook_name`: `{{ alert.playbook }}`

워크플로 시작부에 `if: "'{{ alert.playbook }}' == 'service_restart'"` 안전 게이트를 배치하여, 지정된 플레이북 외의 무단 실행을 차단합니다.

---

## 12. 교차 소스 조회 상태 계약 (Source Status Contract)

수집기(`collector.py`)의 외부 API 조회 연산 결과를 4가지 명시적 상태로 구분하여 데이터 부재에 따른 오판을 차단합니다.

| 수집 상태 (`sources`) | 정의 및 상태 의미 | LLM 분석 및 프롬프트 해석 지침 |
|---|---|---|
| `ok` | API 정상 조회 완료 | 데이터 부재 시 **"이상/침해 흔적 없음"**으로 판정 가능 |
| `unavailable` | API 연동 실패 | 데이터 부재 시 **"조회 실패에 따른 미상 상태"**로 명시 (안심 단정 금지) |
| `unmatched` | 조회는 성공하였으나 해당 소스가 이 호스트 이름을 보유하지 않음 | 데이터 부재 시 **"호스트 이름 불일치로 조회되지 않음"**으로 명시하고, 확인 명령에 이름 대조 항목을 포함 |
| `disabled` | 미연동 설정 상태 | 해당 관측 축 분석 대상에서 제외 |

**`unmatched` 판정 절차:** 조회 결과가 0건인 경우에 한하여 추가 확인을 수행합니다. Loki 는 라벨 값 목록 API(`/loki/api/v1/label/<label>/values`)로, Wazuh 는 동일 조건의 건수 전용 질의(`size: 0`)로 최근 7일 범위를 조회하여 해당 호스트 이름의 등록 여부를 확인합니다. 확인 질의 자체가 실패한 경우에는 판정이 불가하므로 `unavailable` 로 처리합니다. 결과가 존재하는 정상 경로에서는 추가 질의가 발생하지 않습니다.

**도입 배경:** 관측 소스 3종(Zabbix·Loki·Wazuh)이 동일 호스트를 각기 다른 이름으로 식별하는 문제가 랩 환경에서 실측되었습니다(Zabbix `node1` / Loki `vm-target-001.novalocal` / Wazuh FQDN). 이름이 일치하지 않으면 API 는 정상 응답과 함께 빈 결과를 반환하므로, 기존 로직은 이를 조회 성공으로 처리하여 분석 봇이 "로그에 기록 없음"을 사실로 단정하는 결함이 존재하였습니다.

**관측 축 면제 대상:** 해당 관측 축의 데이터가 원천적으로 발생하지 않는 호스트가 존재합니다. 이러한 호스트를 이름 불일치로 판정하면 알림 발생 시마다 분석이 구동되며, 실제 불일치 대응에 사용할 시간당 상한(§8-3)을 선점하게 됩니다. Zabbix 호스트명 패턴을 등록하면 해당 축의 조회를 수행하지 않고 `disabled` 로 기록합니다. 와일드카드는 `*` 를 사용합니다.

| 환경 변수 | 적용 축 | 등록 대상 |
|---|---|---|
| `LOGS_EXEMPT_HOSTS` | Loki | OS 가 존재하지 않는 가상 호스트(인증서 만료 감시 대상, 리포트 값 저장용) |
| `SECURITY_EXEMPT_HOSTS` | Wazuh | 위 가상 호스트 + Wazuh 에이전트 미배포 호스트 |
| `LOG_AXIS_EXEMPT_HOSTS` | 양쪽 | 축별 변수 미지정 시 적용되는 기본값 |

```bash
LOGS_EXEMPT_HOSTS=cert-*,report-*
SECURITY_EXEMPT_HOSTS=cert-*,report-*,customer-*,lab-*,Zabbix server
```

**두 축의 커버리지가 다른 이유:** 로그 축은 Alloy 의 라벨 변환을 통해 복수 컨테이너를 단일 논리 호스트로 귀속시킬 수 있습니다. 반면 Wazuh 는 에이전트가 OS 인스턴스 단위로 배포되므로, 컨테이너를 특정 논리 호스트의 에이전트로 귀속시킬 수단이 없습니다(컨테이너 내부 에이전트 설치는 권장 방식이 아님). 실환경 역시 Wazuh 도입 진행 단계로 에이전트가 일부 서버에만 배포된 상태이므로, 축별 개별 선언이 필요합니다.

**관련 설정:** 라벨 이름이 `host` 가 아닌 환경을 위해 `LOKI_HOST_LABEL` 환경 변수를 제공합니다. 조회 질의와 확인 질의가 동일한 값을 사용합니다.

**호스트 이름 해석 규칙 (`_resolve_label`):** `HOST_LABEL_MAP` 의 명시적 매핑을 최우선으로 적용하고, 미지정 시 인터페이스 `dns` 필드를 사용하되 **FQDN 형태(점 포함)인 경우로 한정**합니다. 조건 미충족 시 Zabbix 호스트명을 사용합니다. 랩 실측 결과 `dns` 필드에 컨테이너 이름(`zabbix-agent2`, `snmpsim`)이 입력된 사례가 확인되었으며, 해당 값은 복수 호스트가 공유할 수 있어 타 호스트의 로그를 조회 대상 호스트의 데이터로 오인할 위험이 존재합니다.

**사전 점검 명령:** 위 판정은 사건 발생 시 해당 호스트 1대에 대해서만 수행됩니다. 에이전트 배포 직후 또는 명명 규칙 변경 후에는 전수 대조가 필요하므로 진단 명령을 제공합니다. 수집기와 동일한 이름 해석 규칙(`HOST_LABEL_MAP` ➔ 인터페이스 `dns` ➔ Zabbix 호스트명)을 사용합니다.

```bash
python3 probe.py names
```

수집 상태 정보는 마스킹 파이프라인을 거쳐 LLM 프롬프트 및 Slack 알림 카드 배지(⚠️ 조회 실패 · ⚠️ 이름 불일치)로 전파되어, 시스템 장애 상황에서의 거짓 안심(False Confidence) 발생을 차단합니다.

---

## 20. 게이트웨이 생존 감시 (Heartbeat)

이상 신호의 발생이 아니라 **정상 신호의 부재**로 게이트웨이 중단을 감지합니다. 게이트웨이가 중단되면 알림은 계속 수신되나 분석이 수행되지 않으며, 이 상태는 어떠한 알림도 발생시키지 않습니다. 실환경에서 알림 채널 1종이 30일간 9,198건을 연속 실패했음에도 인지되지 않은 사례와 동일한 구조입니다.

### 20-1. 전송 방식

`gateway/heartbeat.py` 가 Zabbix sender 프로토콜을 표준 라이브러리로 직접 구현합니다(공식 문서 기준 — 헤더 `ZBXD` + 플래그 1바이트 + 데이터 길이 4바이트 리틀엔디언 + 예약 4바이트, 본문 `{"request": "sender data", "data": [...]}`). `zabbix_sender` 실행 파일에 의존하지 않으므로 설치 환경 제약이 없습니다.

| 환경 변수 | 기본값 | 설명 |
|---|---|---|
| `HEARTBEAT_ZABBIX_SERVER` | (없음) | 수신 Zabbix 서버 주소. 미설정 시 기능 전체 비활성 |
| `HEARTBEAT_ZABBIX_PORT` | `10051` | 트래퍼 수신 포트 |
| `HEARTBEAT_HOST` | (없음) | Zabbix 상의 수신 호스트명. 미설정 시 기능 전체 비활성 |
| `HEARTBEAT_INTERVAL_S` | `60` | 전송 주기 |

### 20-2. 전송 항목

생존 여부만으로는 **게이트웨이는 정상이나 알림이 유입되지 않는 상태**(발행 측 액션·미디어 장애)를 구분할 수 없으므로, 마지막 알림 수신 이후 경과 시간과 처리량을 함께 전송합니다.

| 아이템 키 | 내용 |
|---|---|
| `gateway.alive` | 생존 신호(항상 1) |
| `gateway.uptime` | 프로세스 구동 시간(초) |
| `gateway.since_last_alert` | 마지막 알림 수신 이후 경과(초). 수신 이력이 없으면 기동 시점 기준 |
| `gateway.alerts` / `gateway.incidents` | 수신 알림 수 / 병합된 사건 수(누적) |
| `gateway.analyzed` / `gateway.skipped` | 분석 수행 / 조건 미충족 생략(누적) |
| `gateway.recent_alerts` / `gateway.zbx_events` | 최근 창 수신 알림 수 / 발행 측 생성 이벤트 수 |
| `gateway.recent_alerts[<서버>]` / `gateway.zbx_events[<서버>]` | 위 두 항목의 감시 서버별 값(§20-7) |

### 20-3. Zabbix 측 구성

수신 항목과 트리거는 `ansible/gateway_heartbeat.yml` 로 배포합니다. 트리거는 두 가지 상태를 분리하여 판정합니다.

| 트리거 | 조건 | 심각도 | 의미 |
|---|---|---|---|
| 게이트웨이 신호 끊김 | `nodata(gateway.alive, 3m)=1` | High | 프로세스 중단 또는 전송 경로 장애 |
| 알림 수신 경로 끊김 | `alive=1` 이고 `zbx_events > 3` 이고 `recent_alerts = 0` | Average | 발행 측은 이벤트를 생성했으나 수신 0건 |

`nodata` 창은 전송 주기의 3배로 설정합니다. 1회 누락 시 발화하도록 구성하면 재기동 시마다 알림이 발생합니다.

**"수신 없음"을 무알림 지속 시간으로 판정하지 않는 이유:** 최초 구성 시 `since_last_alert > 30분` 조건을 적용하였으나, 알림이 발생하지 않는 시간대가 정상인 환경에서는 오탐이 발생합니다(랩 환경에서 실제 발화 확인). 본 프로젝트가 해소 대상으로 삼은 알림 노이즈를 새로 생성하는 구성에 해당합니다.

대신 **발행 측 생성량과 수신량을 비교**합니다. 게이트웨이가 동일 창에 대해 Zabbix 가 생성한 문제 이벤트 수를 읽기 전용으로 조회하여(`countOutput`) 자체 수신 건수와 함께 전송하며, 트리거는 두 값을 비교합니다.

| 상태 | 발행 측 | 수신 | 판정 |
|---|---|---|---|
| 정상(무알림 구간) | 0 | 0 | 미발화 |
| 정상(알림 처리 중) | N | N 근사 | 미발화 |
| 경로 장애 | N > 3 | 0 | **발화** |

조회 실패 시 해당 항목을 전송하지 않습니다. 0 을 전송할 경우 "발행 측도 무알림"으로 해석되어 경로 장애가 정상으로 판정됩니다. 임계값 3 은 액션 조건에 의해 일부 이벤트가 대상에서 제외될 수 있는 점을 고려한 여유값입니다.

**⚠ 알림 경로 주의:** 본 호스트의 알림을 게이트웨이 경유로 발송하도록 구성하면, 게이트웨이 중단 시 해당 알림도 함께 소실됩니다. 감시 주체는 감시 대상 외부에 위치해야 합니다.

**⚠ 실환경 적용:** 본 플레이북은 아이템과 트리거를 **생성**하므로 실환경 읽기 전용 접근 원칙에 위배됩니다. 실환경은 동일한 정의를 담당자가 반영합니다.

### 20-7. 감시 서버가 복수인 환경

발행 측 생성량 비교는 **감시 서버마다 따로 수행**합니다. 1개 서버만 대상으로 집계할 경우, 해당 서버가 정상 동작하는 동안 타 서버의 알림 경로가 중단되어도 판정되지 않습니다. 사내 이벤트 수가 정상 범위이므로 조건이 성립하지 않기 때문입니다.

**구성 방식:** 트래퍼 호스트는 1개로 유지하고 아이템 키에 서버명을 부여합니다(`gateway.zbx_events[zabbix-msp]`). 감시 서버 추가 시 아이템과 트리거만 증가하며 호스트 구성은 변경되지 않습니다.

**서버명 일치 요건:** 아이템 키의 서버명은 호스트 명부 `sources` 절의 `name` 및 미디어 타입이 전달하는 `source` 파라미터 값과 일치해야 합니다. 불일치 시 값이 전송되지 않으며, 트리거는 `nodata` 조건에 의해 미발화 상태로 유지됩니다.

**배포:**

```bash
ansible-playbook ansible/gateway_heartbeat.yml -e '{"hb_sources":["zabbix-internal","zabbix-msp"]}'
```

`hb_sources` 미지정 시 서버명 없는 기존 아이템만 생성되며, 이는 감시 서버가 1대인 환경의 구성입니다. 명부에 `sources` 절을 추가한 경우 본 플레이북을 재실행해야 합니다. 게이트웨이가 서버별 키로 전송을 전환하므로, 미실행 시 해당 값을 수신할 아이템이 부재합니다.

### 20-8. 외부 LLM 전송 경로 (Egress)

**단일 출구 원칙:** 외부 LLM 호출은 `gateway/egress.py` 의 `call()` 을 통해서만 수행합니다. 이 모듈을 경유하지 않고 어댑터를 직접 호출하는 코드가 존재해서는 안 되며, 셀프테스트가 `llm.py` 내 어댑터 반복문 잔존 여부를 검사합니다.

**도입 경위 (2026-08-11):** 동시 호출 상한을 `llm.triage_reply` 에 적용하면서 "모든 호출이 통과하는 유일한 경로" 로 판단하였으나, 검증 결과 사실이 아니었습니다. `llm.monthly_reply`(월간 리포트 분석)가 어댑터를 직접 호출하여 동시 수·시간당 총량 양쪽 모두 미적용 상태였으며, 어댑터 폴백 체인도 2벌로 중복되어 있었습니다. 해당 사실은 코드 확인 이전까지 드러나지 않았습니다.

**구조:** 출구를 어댑터 호출 직전으로 이동하였습니다. 마스킹은 호출 측에 유지합니다 — 무엇을 가려야 하는지는 해당 요청의 맥락을 보유한 측만 판단 가능합니다. 열화 시 회신 문구도 호출 측이 생성합니다(트리아지와 리포트는 남길 내용이 상이함).

| 경로 | 용도 구분 | 위중 면제 |
|---|---|---|
| `llm.triage_reply` | `triage` | SEV1 |
| `llm.monthly_reply` | `monthly` | 없음 |
| HolmesGPT 심층조사 | `holmes` | 없음 |

**단일 경로 검사:** `gateway/` 하위 전 모듈을 대상으로 어댑터 직접 호출(`.complete(`) 및 어댑터 반복문 잔존 여부를 셀프테스트에서 검사합니다(`egress.py` 제외). `llm.py` 만 검사할 경우 타 모듈에 신규 경로가 생겨도 검출되지 않으며, 이는 본 항목의 발생 경위와 동일한 구조입니다. 검사 유효성은 의도적 위반 코드 삽입으로 확인하였습니다(2026-08-11).

**미해소 사항:**

| 항목 | 내용 |
|---|---|
| HolmesGPT 반출 | 호출량 집계와 동시·총량 제한은 2026-08-11 적용(어댑터로 감싸 출구 경유). **반출 통제는 다음 단계(§20-9)에서 처리**하며, 그때까지 고객사 대상은 차단을 유지합니다 |
| 시간당 총량의 재기동 초기화 | 호출 이력이 프로세스 메모리에 존재하므로 재기동 시 0으로 초기화됩니다. 배포 등으로 반복 재기동 시 상한이 실효를 상실합니다 |
| 마스킹 대상 범위 | 마스킹은 요청 맥락 기반이므로 해당 사건의 당사자가 아닌 호스트명은 대상에서 누락됩니다(§1-1-9). 출구가 단일화되어 전역 이름 표를 적용할 위치는 확보되었으나 미적용 상태입니다 |
| 위중 사건 면제 | SEV1 은 총량 미적용이며 별도 상한이 없습니다. SEV1 자체가 폭주하는 경우 총량 제어가 무력화됩니다 |

### 20-8-1. 상한 설정

| 환경 변수 | 기본값 | 내용 |
|---|---|---|
| `LLM_MAX_CONCURRENCY` | 3 | 동시에 진행할 수 있는 LLM 호출 수 |
| `LLM_QUEUE_WAIT_S` | 60 | 자리를 기다리는 상한. 초과 시 열화 모드로 회신 |

**적용 위치:** `llm.triage_reply` 의 어댑터 호출 직전입니다. 발동 게이트가 아닌 이 지점을 선택한 이유는, 봇·리포트·데모 스크립트의 모든 호출이 통과하는 유일한 경로이기 때문입니다. 게이트에 적용할 경우 게이트를 경유하지 않는 호출(사람 요청에 의한 강제 분석, 월간 리포트 분석)이 상한 밖에 위치합니다.

| `LLM_MAX_PER_HOUR` | 200 | 최근 1시간 호출 수 상한. SEV1 은 미적용 |

### 21-2. `guard` — 제한만 떼어낸 형태

`egress.call` 의 계약은 `(system, user) -> str` 입니다. 게이트웨이가 전문을 직접 구성하는 경로에는 적합하나, **외부 도구의 요청을 그대로 중계하는 경로**에는 맞지 않습니다. 다중 턴 `messages` 배열·도구 목록·토큰 상한을 전달할 자리가 없습니다.

계약을 확장하는 대신 **제한 부분을 컨텍스트 매니저로 분리**하였습니다.

```python
with egress.guard("proxy", max_per_hour=2000) as record:
    record()                    # 실제 발신 직전에 계수
    status, resp = await forward(masked, headers)
```

`guard` 는 자리를 확보하고, 확보 실패 시 `Blocked` 를 발생시키며, 종료 시 반환합니다. 내부에서 수행하는 작업의 형태는 관여하지 않습니다.

**`call` 도 이 구현을 사용합니다.** 코드를 복제한 것이 아니라 단일 구현을 공유하는 형태이므로, 제한 규칙 변경 시 양쪽에 동시 반영됩니다. 복제할 경우 한쪽만 수정되어 어긋나는 상태가 발생합니다(본 문서 §8-3 의 사유별 한도가 그 사례). 분리 후 기존 검사 336건이 전부 통과함을 확인하였습니다.

**시간당 총량만 분리 가능합니다** — `max_per_hour` 인자. 동시 호출 제한은 공유합니다. 스레드 점유와 429 를 방지하는 장치이므로 분리 시 합산되어 실효가 없습니다.

**보호를 이 지점에 두는 이유:** 호출이 발생하는 지점은 1개소이므로, 여기서 제어하면 게이트 규칙 수와 무관하게 미적용 경로가 발생하지 않습니다. 게이트 분기마다 개별 배치할 경우 규칙 추가 시 누락이 발생하며 누락은 관측되지 않습니다(변경 전 구조에서 5종 중 3종 미적용, §8-3).

**동시 수와 시간당 총량을 모두 두는 이유:** 두 지표는 서로 다른 대상을 측정합니다. 동시 수는 순간 집중을, 총량은 누적 소모를 제어합니다. 동시 3건 유지 시에도 장애가 장시간 지속되면 시간당 최대 약 720건에 도달하므로 총량 제어가 별도로 필요합니다.

**SEV1 미적용:** 위중 사건은 총량 상한을 적용하지 않습니다. 폭주 시점에 위중 사건이 차단되면 상한이 장애를 확대합니다. 단 소비량 집계에는 포함합니다.

**기본값 3의 산정 근거:** 실측 응답 시간 중앙값 14.1초(§5 참조)를 기준으로, 3건 동시 진행 시 시간당 처리 가능량은 약 760건입니다. 실환경 90일 이벤트 43,015건(일 478건)에 대해 발동 조건 통과분만 호출 대상이 되므로 정상 부하에서는 대기가 발생하지 않습니다. 상한은 정상 처리량 제한이 아니라 **집중 시점의 동시 호출 수 억제**를 목적으로 합니다.

**대기 상한 60초를 두는 이유:** 무기한 대기 시 스레드 풀이 대기 상태로 점유되어 Slack 게시·Keep 기록 등 다른 작업이 지연됩니다. `asyncio.to_thread` 는 기본 스레드 풀을 공유하므로 대기가 누적되면 파이프라인 전체가 정지합니다.

**초과 시 동작:** 예외를 발생시키지 않고 열화 모드로 회신합니다. 회신 본문에 대기 포기 사실을 명시하며, 코드 판정(만성/신규, 병합 사유)은 그대로 포함됩니다.

**총량 200 의 산정 근거:** 실환경 90일 이벤트 43,015건은 시간당 평균 약 20건이며, 인시던트 병합 후에는 이를 하회합니다. 상한은 정상 부하의 약 10배에 해당하므로 통상 운영에서는 도달하지 않으며, 다수 호스트 동시 장애 시에만 작동합니다. 처리량 배분이 아니라 **폭주 제동**을 목적으로 하므로 여유를 두어 설정합니다.

**관측:** `gateway.llm_peak_inflight`(최고 동시 호출 수)·`gateway.llm_queue_timeouts`(대기 포기 누적)·`gateway.llm_hour_blocked`(총량 차단 누적)·`gateway.llm_calls_1h`(최근 1시간 호출 수)를 생존 신호와 함께 전송합니다. 대기 포기는 알림 처리 자체는 정상이고 분석 깊이만 저하되는 상태이므로 화면상 식별이 불가능합니다. 최근 1시간 내 증가분이 존재하면 트리거가 발화합니다.

### 20-9. 심층조사 도구의 반출 통제 — **구현 완료 (2026-08-12, §23)**

마스킹 지점은 게이트웨이와 도구 사이가 아니라 **도구와 모델 사이**입니다. 해당 도구는 전달받은 호스트명으로 감시 서버를 직접 조회하므로, 게이트웨이 발신 문장을 마스킹하는 방식은 성립하지 않습니다(마스킹하면 조회 불가, 마스킹해도 도구 자체 수집분은 통제 밖).

구현 내용은 §23. 잔여는 랩 종단 검증과 고객사 차단 완화 판단입니다.

### 20-5. 알림 경로 분리 (Independent Notification Path)

**본 트리거의 알림을 게이트웨이 경유로 발송하면 게이트웨이 중단 시 알림도 함께 소실됩니다.** 랩 환경 점검 결과 활성 트리거 액션이 게이트웨이 웹훅 1종뿐이었으며, 해당 상태에서는 게이트웨이 중단 사실이 어디에도 통보되지 않습니다.

이에 Zabbix 서버가 Slack 으로 직접 발송하는 독립 경로를 구성합니다. 웹훅 스크립트는 Zabbix 서버 내부에서 실행되므로 게이트웨이 동작 여부와 무관합니다.

| 구성 요소 | 내용 |
|---|---|
| 미디어 타입 | `Selfwatch Slack` (webhook) — `ansible/files/zabbix_media_selfwatch.js` |
| 액션 조건 | 호스트 = `kinx-gateway` 단일 조건. 미지정 시 전체 알림이 본 경로로 유입되어 채널이 무의미해짐 |
| 발송 채널 | **봇 사용 채널과 분리.** 게이트웨이 중단 시 봇 채널은 무음 상태가 되며, 그 무음 자체가 증상이므로 동일 채널에 경고를 배치하면 두 상태가 혼재함 |
| 크리덴셜 | 실행 시 환경 변수로 주입(`SELFWATCH_SLACK_BOT_TOKEN`·`SELFWATCH_SLACK_CHANNEL`). 미설정 시 플레이북 중단 |

**Slack 응답 검증:** Slack API 는 처리 실패 시에도 HTTP 200 을 반환하므로, 스크립트에서 응답 본문의 `ok` 필드를 확인하고 거짓일 경우 예외를 발생시킵니다. 미검증 시 발송 실패가 성공으로 기록됩니다.

### 20-6. 구성 시 확인된 무음 실패 사례 (2026-08-11)

본 구성 과정에서 **API 응답과 화면 설정은 정상이나 알림이 발송되지 않는** 상태가 3회 발생하였습니다. 관측 파이프라인 점검 시 참고 사항으로 기록합니다.

| 현상 | 원인 | 확인 방법 |
|---|---|---|
| 모듈이 `changed` 를 반환했으나 액션 미생성 | 호스트 조건의 `value` 는 호스트명이 아닌 **hostid** 를 요구함 | 생성 후 재조회 검증 단계 추가 |
| `action.get` 조회 시 액션 미표시 | **읽기 전용 토큰 권한 제약** — 액션이 참조하는 사용자에 대한 권한 부재 시 목록에서 제외됨. 실제로는 정상 생성됨 | DB 직접 조회(`actions` 테이블) |
| 액션·조건·사용자 미디어 정상이나 발송 미생성 | 미디어 타입에 **메시지 템플릿 부재**. `default_msg` 사용 시 알림 자체가 생성되지 않음 | `alerts` 테이블 오류 컬럼 — `No message defined for media type.` |

세 건 모두 오류 로그가 발생하지 않았으며, `alerts` 테이블 직접 조회로 확인되었습니다.

### 20-4. 랩 검증 결과 (2026-08-11)

1. 프로토콜 전송 확인 — 서버 응답 `processed: 7; failed: 0`
2. 게이트웨이 중단 후 3분 경과 시점에 `게이트웨이 신호 끊김` 트리거 발화 확인(High)
3. 게이트웨이 재기동 후 해당 문제 자동 해소 확인
4. **독립 경로 Slack 발송 확인** — 발생·해소 각각에 대해 `alerts` 테이블 상태 1(성공) 기록. 스크립트가 Slack 응답의 `ok` 를 검증하므로 성공 상태는 Slack 수신 완료를 의미함

---

---

---

## 22. 전역 이름 표 (Name Table)

### 22-1. 도입 배경

마스킹은 해당 사건의 호스트 정보에서 이름을 추출하여 등록합니다(`masking._register_host`). 따라서 **사건 당사자가 아닌 호스트명은 치환되지 않습니다.** 로그 라인에 타 서버명이 포함되면 원문 그대로 전송되며, 장애 규모가 클수록 해당 라인이 증가하므로 규모에 비례하여 노출이 확대되는 구조입니다.

확인된 누락 경로 3종:

| 경로 | 내용 |
|---|---|
| 조회 이름 미등록 | 해석된 Loki 라벨·Wazuh 에이전트명이 컨텍스트에 미포함이었으나 로그 본문에는 해당 이름이 존재함 |
| 폴백 호스트명 | 수집 전건 실패 시 호스트 객체가 비어 등록이 0건이 되나 `incident.host` 는 전송됨. 마스킹이 항등 함수로 동작 |
| 제3 호스트명 | 로그·보안 설명·경로에 등장하는 타 호스트명은 등록 대상 자체가 아니었음 |

앞의 2종은 `masking._register_context` 에서, 3종은 본 표가 처리합니다.

### 22-2. 치환 규칙

**경계 일치 + 대소문자 무시 + 긴 항목 우선.**

종전에는 단순 문자열 치환이었습니다. `db01` 등록 시 `mydb01`·`db011` 내부에서도 치환되어 문장이 손상되고, 반대로 `DB01` 은 미치환되었습니다.

경계는 `` 가 아니라 비단어 문자 lookaround 를 사용합니다. 호스트명에 `-`·`.` 이 빈번한데 `` 는 해당 위치를 다르게 판정합니다. [Presidio 의 deny-list 인식기](https://github.com/microsoft/presidio/blob/main/presidio-analyzer/presidio_analyzer/pattern_recognizer.py)와 동일한 형태이며, 대소문자 무시도 해당 구현의 기본값을 따랐습니다.

긴 항목을 앞에 배치하는 순서가 필요합니다. 정규식 선택지는 선행 항목이 우선 매칭되므로 `report-Customer-B` 가 `customer-b` 보다 앞에 있어야 전체가 치환됩니다. **랩 실측상 이름 26개 중 5개(19%)가 타 이름의 부분 문자열**이므로 실제로 발생하는 조건입니다.

**역치환 시 대소문자:** 등록 원문과 다른 대소문자로 출현한 위치는 정규형으로 복원됩니다. 왕복이 문자 단위로 동일하지 않으나, 회신 본문은 토큰만 포함하므로 운영상 영향이 없습니다. 해당 동작은 셀프테스트로 고정하였습니다.

### 22-3. 오탐 처리 — 항목을 제외하지 않음

3자 이하이거나 영문 소문자 낱말인 이름은 문장에 자연히 출현할 수 있습니다. 이러한 항목을 표에서 제외할 경우 **해당 이름이 원문으로 전송**되므로 제외하지 않고 `risky()` 로 보고만 합니다. 실제 오탐이 확인되면 명부 파일의 `allow:` 절에 기재하여 예외 처리합니다. Presidio 의 allow-list 와 동일한 방식입니다.

### 22-4. 생성과 갱신

사람이 작성하는 파일이 아닙니다. 코드가 조회하여 생성합니다.

| 출처 | 조회 |
|---|---|
| Zabbix | 명부 `sources` 절의 서버별 `host.get` (호스트명·표시명·DNS·호스트그룹) |
| Loki | 라벨 값 목록 (7일) |
| Wazuh | `agent.name` 집계 1회. 이름별 존재 확인만 있었으므로 신규 구현. 호스트당 1요청을 회피 |
| 명부 파일 | `hosts[].name/loki/wazuh` — 조회로 포착되지 않는 이름(경보 이력이 없는 에이전트 등) |

기동 시 캐시를 먼저 읽고 `NAMETABLE_REFRESH_S`(기본 3600) 주기로 갱신합니다. 조회에 수 초가 소요되므로 별도 스레드에서 수행합니다.

**실패 시 동작:** 출처 1종이 실패해도 나머지로 생성합니다. 전건 실패 시 **직전 표를 유지**합니다 — 빈 표로 대체하면 그 시점부터 치환이 중단되며 그 사실이 드러나지 않습니다. 표가 없어도 종전 동작(맥락 기반 등록 + IP 정규식)으로 동작합니다.

### 22-5. 정확도 검증

```bash
python3 probe.py nametable
```

표 생성 후 위험 항목 비율, 미탐 수, 오탐 수를 출력합니다. 이름은 화면에만 표시하며 수치만 문서에 기재합니다.

| 환경 | 이름 수 | 위험 항목 | 미탐 | 오탐 |
|---|---|---|---|---|
| 랩 | 38 (Zabbix 36·Loki 8·Wazuh 3) | 7 (18%) | 0/76 | 0/7 |
| 실환경(사내) | 346 (Zabbix) | 7 (2.0%) | 0/346 | 0/8 |
| 실환경(MSP) | 159 (Zabbix) | 1 (0.6%) | 0/159 | 0/8 |

실환경 위험 항목 비율이 랩의 1/9 입니다. 랩은 호스트 15대에 접두를 공유하는 이름이 많아 부분 문자열 비율이 높았습니다.

**조치 1건 — 3자 이하 한글 그룹명.** 낱말로 단독 출현할 때 일반 문장에서 치환되는 것을 확인하였습니다(합성어 내부는 한글이 단어 문자이므로 안전). 호스트가 아닌 그룹명이며 일반 낱말이므로 노출 위험이 낮은 반면 문장 손상은 확실하므로 `allow:` 절에 추가합니다.

MSP 세트에서도 동일 유형 1건(영문 낱말과 같은 그룹명)이 확인되어 `allow:` 대상입니다. 측정 상세는 `private/docs/nametable_accuracy_20260812.md`.

---

## 23. 외부 도구용 마스킹 수신 지점 (Proxy)

### 23-1. 무엇을 해결하는가

심층조사 도구는 전달받은 호스트명으로 **감시 서버를 직접 조회하고 그 결과를 자체 키로 모델에 전송**합니다. 게이트웨이가 발신 문장을 마스킹해도 도구가 자체 수집한 데이터는 통제 범위 밖이며, 호스트명을 가명화하면 조회가 불가능하여 기능이 성립하지 않습니다. 따라서 종전에는 고객사 알림에 대해 해당 도구를 **차단**하였습니다.

### 23-2. 구조

```
  [심층조사 도구] ──ANTHROPIC_API_BASE──▶ [게이트웨이 POST /v1/messages]
        │  ▲                                    │ ① 이름 표로 마스킹
        │  │                                    │ ② 동시·시간당 제한, 집계
        │  │                                    ▼
        │  │                              api.anthropic.com
        │  │                                    │  (모델은 가명만 관측)
        │  │                                    ▼
        │  └──③ 역치환(text + tool_use.input)───┘
        ▼
  [Zabbix · Loki]        ← 조회는 내부망에서 실명으로 수행
```

**③이 핵심입니다.** 모델은 가명으로 도구 호출을 생성하며, 이를 그대로 반환하면 도구가 존재하지 않는 이름으로 조회하여 빈 결과를 받고 이를 "이상 없음" 으로 해석합니다. 응답의 `tool_use.input` 까지 역치환해야 조사가 완주합니다.

**프로토콜 근거:** LiteLLM 은 `ANTHROPIC_API_BASE` 를 받고 해당 주소에 `/v1/messages` 를 자동 부착합니다([공식 문서](https://docs.litellm.ai/docs/providers/anthropic)). 도구는 LiteLLM 을 내부적으로 사용하므로 환경 변수 지정만으로 경유가 성립하며 도구 코드 수정이 불필요합니다.

### 23-3. 마스킹 범위

| 방향 | 대상 |
|---|---|
| 요청 | `system`, `messages[].content`(문자열·`text.text`·`tool_result.content`·`tool_use.input` 값), `tools[].description` |
| 응답 | `content[].text`, **`content[].tool_use.input` 값** |

**프로토콜 키는 제외합니다** — `type`·`role`·`model`·`name`·`id`·`tool_use_id` 등. 전체 문자열을 무차별 치환할 경우 호스트명이 `Test`·`text` 와 같은 일반 낱말일 때 해당 자리가 변형되어 도구 호출이 실패합니다. MSP 세트에 `Test` 그룹명이 실재합니다.

### 23-4. 토큰은 이름에서 산출

`Masker` 의 기본 방식은 등록 순서에 따른 일련번호(`[host-1]`)이나, 본 경로에서는 **이름의 해시**를 사용합니다(`[host-a43b0e15c800]`).

이름 표는 주기 갱신되며 정렬 기준이 길이 내림차순입니다. 조사 1회가 모델을 수십 회 호출하는 동안 갱신이 발생하면 순번이 이동하여 **동일 토큰이 다른 호스트를 지시**하게 됩니다. 모델은 이전 대화의 토큰과 현재 토큰을 동일 대상으로 해석하며, 오류 없이 분석 결과만 왜곡됩니다.

**해시 자릿수 (2026-08-13 정정).** 종전 6자리(16진)는 약 1,678만 가지로, 실제 탐색 결과 이름 3,708개 지점에서 충돌이 발생했습니다(`host-1422.kinx.net` 과 `host-3707.kinx.net` 이 동일 토큰 `2f64e3`). 충돌 시 역치환 표가 덮여 **다른 호스트명이 반환되며, 수신자는 이를 사실로 인지**합니다. 실환경 사내 호스트만 321대이고 이름 표는 호스트·그룹·Loki 라벨·Wazuh 에이전트명을 합산하므로 해당 규모에 근접합니다. 자릿수를 **12자리**(약 2.8×10^14 가지)로 확대하였으며, 환경변수 `LLM_TOKEN_HEX` 로 조정합니다. 자릿수 확대로도 충돌은 원리상 잔존하므로 `proxy.token_collisions()` 가 표 생성 시점에 검출하여 오류 로그로 남깁니다.

**IP 도 동일 방식으로 산출합니다.** `Masker.mask()` 는 조회 중 발견한 IP 를 그 자리에서 등록하는데, 기본 경로에서는 일련번호가 부여됩니다. 1회차에 IP 가 2개면 `[ip-1]`·`[ip-2]` 이나 3회차에 두 번째만 등장하면 그것이 `[ip-1]` 이 되어 **동일 토큰이 다른 대상을 지시**합니다. `Masker(token_fn=…)` 로 이름 기반 산출 함수를 주입하며, `build_masker()` 가 이를 적용합니다. 트리아지 경로는 요청 단위 처리이므로 종전 일련번호 방식을 유지합니다.

### 23-5. 역치환 실패 검출

모델이 토큰을 변형하거나 표에 없는 토큰을 생성한 경우 역치환이 누락됩니다. 응답 반환 전 `tool_use.input` 에 토큰 형태가 잔존하면 해당 요청을 **502 로 실패 처리**합니다. 조용한 오답보다 명시적 실패가 우선입니다.

### 23-6. 실패 처리

| 상황 | 응답 |
|---|---|
| 인증 실패 | 401 |
| `stream: true` | 400. 조각 경계에서 토큰이 분할되어 역치환이 불가능함 |
| 이름 표 공백 | 503. 마스킹을 보장할 수 없으므로 차단. **호출자가 누구라고 신고했는지 보지 않는다** — 그 값은 호출자가 정하므로 근거가 못 된다(2026-08-18 정정) |
| 이름 표 공백 + `PROXY_ALLOW_UNMASKED=1` | 통과하되 오류 기록. 호출자의 주장이 아니라 운영자가 설정 파일에 적은 것이다 |
| 제한 초과 | 429 |
| 역치환 실패 | 502 |

### 23-7. 통제의 경계

**우회는 자격 증명으로 차단합니다.** 도구에 상류 키를 부여하지 않고 게이트웨이가 자체 키로 상류를 호출합니다. 기준 주소를 되돌려도 인증이 성립하지 않으므로, 네트워크 정책보다 강하며 설정 오류로 우회되지 않습니다.

**이름 표 범위 밖의 식별자는 통제되지 않습니다.** 표는 호스트명·그룹명과 IP(정규식)를 인식합니다. 계정명·URL 경로·티켓번호·로그 본문의 조직명은 대상이 아닙니다. **고객사 차단 완화는 해당 범위에 대한 별도 판단 이후에 수행합니다** — 본 지점의 동작만으로 완화할 경우 "마스킹된다" 는 표현이 실제 보호 범위를 초과하여 해석됩니다.

### 23-8. LiteLLM + Presidio 미채택 사유

동일 목적의 구성(`masking/` — LiteLLM 프록시 + Presidio 가드레일)이 선행 검토되었으나 2026-08-12 미채택하였습니다.

1. **인식기가 랩 명명 규칙 정규식** — `vm-*`·`node[0-9]+`·`kinx-*`·`customer-*` 로 구성되어 실환경 이름(FQDN·영문 대문자·한글 그룹명)을 포착하지 못합니다.
2. **필수 성질 2종이 공식 문서에 미명시** — 개체별 고유 토큰 부여 여부와 `output_parse_pii` 의 도구 호출 인자 복원 여부입니다. 전자가 성립하지 않으면 호스트 간 관계 분석이 불가능하고, 후자가 성립하지 않으면 조사가 첫 조회에서 중단됩니다.

자체 구현은 실환경 505개 이름으로 정확도를 측정한 이름 표(§22)를 그대로 사용하며, 컨테이너 3종 추가가 불필요합니다.

### 23-9. 설정

```bash
# 도구 측 — 진짜 상류 키를 주지 않는다
export ANTHROPIC_API_BASE="http://<게이트웨이>:8800"
export ANTHROPIC_API_KEY="<게이트웨이 토큰>"
```

| 환경 변수 | 기본값 | 내용 |
|---|---|---|
| `LLM_UPSTREAM_URL` | `https://api.anthropic.com` | 상류 주소 |
| `LLM_PROXY_MAX_PER_HOUR` | 2000 | 본 경로 전용 시간당 상한 |
| `LLM_PROXY_TIMEOUT_S` | 120 | 상류 응답 대기 |

**상한을 분리한 사유:** 조사 1회가 모델을 약 30회 호출합니다(벤치마크 실측). 기본 상한 200 을 공유할 경우 조사 6~7회로 예산이 소진되어 실시간 트리아지가 차단됩니다. **동시 호출 제한은 공유합니다** — 스레드 점유와 429 를 방지하는 장치이므로 분리 시 의미가 없습니다.

### 23-10. 심층조사 도구 기동

도구는 `lab/holmes/docker-compose.yml` 로 기동합니다. 종전에는 VM 에서 `docker run` 으로 직접 실행하여 기동 명령과 툴셋 설정이 리포지토리에 존재하지 않았습니다(2026-08-12 확인). 해당 VM 이 소실되면 재현이 불가능한 상태였습니다.

```bash
cd lab/holmes
export ANTHROPIC_API_BASE="http://127.0.0.1:8800"   # 게이트웨이 수신 지점
export GATEWAY_TOKEN="..."                          # 도구 → 게이트웨이 인증
export ZABBIX_URL="http://127.0.0.1:8080"
export ZABBIX_HOSTPORT="127.0.0.1:8080"
export LOKI_URL="http://127.0.0.1:3100"
export WAZUH_INDEXER_URL="https://..."
export WAZUH_BASIC_AUTH="..."                       # base64(user:pass)
export ZABBIX_TOKEN="..."                           # 조회 전용
docker compose up -d
```

**진짜 상류 키를 부여하지 않습니다.** `ANTHROPIC_API_KEY` 자리에 게이트웨이 토큰이 들어갑니다. 도구가 기준 주소를 되돌려도 상류 인증이 성립하지 않으므로 우회가 차단됩니다(§23-7).

`config.yaml` 은 읽기 전용으로 마운트하며 주소·자격 증명은 전부 환경 변수 참조입니다. 도구가 생성하는 상태 파일(`config_hashes`·`toolsets_status.json`)은 컨테이너 내부에만 존재하며 재생성 가능합니다.

**툴셋 변경 후에는 캐시를 새로 읽혀야 합니다** — 커스텀 툴셋이 캐시 로드 시 누락되어 직전까지 동작하던 조사가 실패한 사례가 있습니다(벤치마크 기록).

### 23-11. 랩 종단 검증 결과 (2026-08-12)

도구 코드 수정 없이 환경 변수 지정만으로 경유가 성립하였습니다.

| 항목 | 값 |
|---|---|
| 중계 요청 | 16건 (조사 1회) |
| 치환 자리 합계 | 560 (요청당 평균 35.0) |
| 치환 0건 요청 | 0 |
| 역치환 실패·제한 거절 | 0 |
| 소요 | 1분 49초 |

**품질 판정: 통과.** 벤치마크(2026-07-28, 비마스킹)와 동일 시나리오·동일 모델로 비교하여
마스킹 여부만 변수로 두었습니다. 근본 원인(디스크 I/O 포화로 인한 복제 지연, DB 고장 아님),
iowait 와 CPU 구분, 금지 조치 권고를 모두 동일 수준으로 산출하였습니다.

**저하가 없던 이유:** 인과를 구성한 것은 호스트명이 아니라 지표 값과 시간축이며 해당 값은
치환 대상이 아닙니다. 도구가 감시 서버에서 실제 데이터를 조회하므로 이름으로 역할을 추정할
필요가 없습니다.

**스트리밍은 사용되지 않았습니다** — 거절 경로가 한 번도 동작하지 않았습니다. SSE 중계는
현 시점에서 불필요합니다.

**신규 의존:** 게이트웨이 중단 시 심층조사도 불가합니다. 차단되는 방향의 실패이나 종전에는
없던 결합입니다.

상세는 `private/docs/proxy_verification_20260812.md`.

---

## 24. 판정 이력 저장소 (History Store)

### 24-1. 무엇을 담는가

`GATEWAY_STORE_FILE`(기본 `~/.kinx-gateway/history.db`) 의 SQLite 파일에 세 가지를 담습니다.

| 테이블 | 내용 | 쓰는 곳 |
|---|---|---|
| `judgment` | 사건별 판정 — 식별자·지문·폴백 키·호스트·영역·유형·건수·심각도·만성 판정·게이트 발동 여부와 사유·조회 상태·출처(자동/사람 요청)·사건 발생 시각·모델·열화 여부·소요·**분석 결론 본문**·변화 판정·과거 결론 사용 여부·주석 식별자 | 게이트 판정 직후 1행, 분석 뒤 갱신 |
| `feedback` | 사람이 남긴 판정 확인·정정 (축별, 이력) | Keep 버튼·재분석 요청 |
| `route` | 알림 1건의 라우팅 판정 (중복 포함) | 웹훅 수신 시 |
| `seen` | 중복 판정 창 | 알림 수신 시 |
| `call` | 외부 호출 시각·용도 | 발신 직전 |

`judgment` 는 **어떤 외부 발신보다 먼저** 씁니다. Keep 카드에 실을 식별자가 여기서 나오고,
분석 중 프로세스가 죽어도 판정이 남습니다.

### 24-2. 왜 필요한가

**판정 이력.** 봇이 얼마나 맞히는지 측정할 재료가 없었습니다. Keep 에 문자열로만 남아 있어 집계가 어렵고, 게이트가 스킵한 사건은 카드도 안 올라갑니다. 판정 정확도 정량 측정(§2-4-1)과 만성 장애의 과거 결론 연계(§2-4-2)가 이 표를 전제로 합니다.

**휘발 상태.** 중복 판정 창·시간당 호출 창·발동 사유 집계가 프로세스 메모리에만 있어 재기동 시 초기화되었습니다. 배포로 반복 재기동하면 중복 억제가 풀리고 호출 상한이 사실상 무효가 됩니다. 2026-08-11 하루에 게이트웨이를 10회 이상 재기동한 사례가 있습니다.

### 24-3. 못 쓸 때의 동작

저장소가 열리지 않으면 **종전 동작(메모리)으로 내려갑니다.** 기능은 축소되지 않고 그 사실이 기록됩니다.

- 중복 판정: 조회 실패 시 **처음 보는 것으로 처리**합니다. 저장소 때문에 알림이 막히면 안 됩니다.
- 호출 상한: 메모리 창으로 판정합니다.
- 판정 이력: 기록 실패가 사건 처리를 막지 않습니다.

### 24-4. 스키마 판올림

판정 식별자(`id INTEGER PRIMARY KEY AUTOINCREMENT`)는 `ALTER TABLE` 로 붙일 수 없어(SQLite 제약) 표를 한 번 다시 만듭니다. 기동 시 `judgment` 에 `id` 컬럼이 없으면 새 표로 옮기고(`INSERT SELECT` → `DROP` → `RENAME`) 기존 행을 보존합니다. 그 뒤의 컬럼 추가는 멱등 `ALTER` 이며, `duplicate column` 오류는 성공으로 흡수합니다 — `mark_judgment.py` 가 같은 파일을 별도 프로세스로 열기 때문입니다.

`AUTOINCREMENT` 를 쓰는 이유는 **식별자 재사용을 막기 위해서**입니다. 없으면 표가 비었을 때 1부터 다시 매겨지고, Keep 카드에 남은 옛 식별자가 남의 판정을 가리킵니다.

### 24-5. 보관

| 대상 | 기한 | 근거 |
|---|---|---|
| 판정 행 | `GATEWAY_STORE_KEEP_DAYS`(기본 90) | 지표 산출 창 |
| 분석 결론 본문 | `GATEWAY_STORE_SUMMARY_DAYS`(기본 30) | 아래 24-6 |
| 라우팅 기록 | 판정 행과 동일 | 라우팅 분모 |
| 호출 기록 | 2일 | 시간당 창 판정에만 쓰임 |

정리는 기동 시 1회가 아니라 `GATEWAY_STORE_PRUNE_S`(기본 6시간) 주기로 돕니다. 기동 때만 지우면 오래 떠 있는 프로세스는 영원히 안 지우고, 그러면 보관 기한이 못 지키는 약속이 됩니다.

### 24-6. 서술을 담는 데 따르는 취급

`summary` 는 LLM 이 쓴 자유 서술이라 로그 라인·경로·계정명이 구조 없이 섞여 들어갑니다. 같은 디렉토리의 기존 파일(대기 목록·이름 표)에도 실명이 있지만 그것들은 구조화된 값이었습니다.

- 디렉토리 `0700`, 파일 `0600`(`-wal`·`-shm` 포함). 같은 기계의 다른 사용자에게 열어 두지 않습니다.
- **행보다 서술을 먼저 지웁니다.** 지표는 구조화 컬럼만으로 산출되므로, 사본이 유출돼도 서사는 나가지 않습니다.
- **프롬프트는 저장하지 않습니다.** 결론만 남깁니다. 프롬프트에는 로그 원문이 통째로 들어 있습니다.

---

## 25. 판정을 쓰는 세 기능 (Quality · Prior · Feedback)

§24 의 판정 이력을 실제로 읽는 쪽입니다. 로드맵 §2-4-1(정확도 측정)·§2-4-2(과거 결론
연계)·§2-4-3(소명과 정정)·§2-4-5(주석 발행)에 해당합니다.

### 25-1. 기록 시점을 앞으로 당긴 이유

전에는 판정을 사건 처리 **맨 끝**에 남겼습니다. 그러면 Keep 카드에 실을 식별자가 없어
사람이 그 판정을 가리킬 방법이 없고, 분석 도중 프로세스가 죽으면 판정이 통째로 사라집니다.

지금은 **게이트 판정 직후**에 행을 만들고 식별자를 받습니다. 분석 결과는
`store.finish()` 가 채웁니다. 저장은 블로킹이므로 `asyncio.to_thread` 로 감쌉니다 —
크리티컬 패스 한가운데에서 WAL 쓰기와 전역 잠금을 잡으면 다른 사건의 타이머와 수신
웹훅까지 함께 기다립니다.

`total_s` 가 빈 행은 `finish` 가 못 불린 것(재기동 등)입니다. 지표에서 **0 으로 세지 않고
제외하며, 제외 건수를 함께 냅니다.**

### 25-2. 라벨 수집 — 버튼 두 개와 공짜 라벨

`keep/workflows/judgment_confirm.yml` · `judgment_correct.yml` 이 카드에서 "판정 확인" ·
"판정 정정" 을 띄웁니다. 승인 계층을 새로 만들지 않았습니다. 조치·리포트 발송·규칙 교체·
재분석에 쓰던 것의 다섯 번째 적용입니다.

**두 개인 이유.** 정정만 두면 불만만 쌓여 분모가 없습니다. 오답 12건은 셀 수 있어도
정확도는 못 냅니다. 게다가 라벨이 오류 쪽으로 치우칩니다 — 귀찮은데도 버튼을 누르는
사람은 대개 틀렸다고 생각한 사람입니다.

**축은 지금 하나(`overall`)만 씁니다.** 게이트·병합·원인으로 나누면 버튼이 여섯 개가
되어 라벨이 더 줍니다. 저장 구조는 축을 이미 갖고 있으므로 나중에 이력을 안 버리고
쪼갤 수 있습니다.

**안전 게이트를 워크플로 `if:` 가 아니라 SSH 명령 안에 둡니다**(`test -n '{{ alert.judgment_id }}'`).
`if:` 로 거르면 건너뛴 실행이 성공처럼 보여서, 판정 식별자가 없는 카드에서 눌러도 사람은
라벨이 남은 줄 압니다.

**공짜 라벨.** 사람이 스킵 카드에서 "지금 분석" 을 누른 것은 **게이트 판정이 틀렸다는
위조 불가능한 신호**입니다. `analyze_now.py` 가 같은 지문의 직전 판정에
`feedback(gate, ok=0)` 을 자동으로 남깁니다. 아무에게도 새 행동을 요구하지 않고 모입니다.

`mark_judgment.py` 는 식별자와 지문을 **함께** 받아 어긋나면 거부합니다. Keep 은 지문으로
카드를 접으므로, 카드가 최신 판정으로 덮인 뒤에도 화면에 옛 식별자가 남아 있을 수
있습니다. 잘못된 인자·저장소 미가동은 종료코드를 0 이 아닌 값으로 돌려 Keep 실행 이력에
실패로 남깁니다.

### 25-3. 라우팅 기록

판정 이력에는 `route == "triage"` 인 알림만 남습니다. `digest`·`dashboard_only`·
`remediate`·`drop` 은 한 행도 안 남으므로, 판정 이력만 보면 **라우팅 타당성을 잴 수
없습니다.** 그래서 웹훅 수신 시 `route` 표에 1행씩 남깁니다.

**중복으로 걸린 알림도 남깁니다.** 안 남기면 분모에서 조용히 빠집니다. 이를 위해 중복
판정보다 심각도·경로 산정을 먼저 합니다.

### 25-4. 판정 주석 (§2-4-5)

사건 확정 시 Grafana 조직 주석을 발행합니다. `dashboardUID` 를 생략하면 모든 대시보드에서
보이며, 오픈소스 배포판에서 됩니다 —
[Annotations HTTP API](https://grafana.com/docs/grafana/latest/developers/http_api/annotations/).

- **배경 태스크로 돕니다.** 주석을 30초 안에 봐야 할 사람은 없습니다.
- **`time` 은 밀리초이고 사건 발생 시각입니다.** 초로 보내면 1970년에 찍혀 화면에서 조용히
  사라집니다. 분석 시각을 쓰면 디바운스와 분석 시간만큼 밀려 지표 스파이크 옆에 있지
  않습니다. 이 기능의 가치가 거기 달렸습니다.
- `timeEnd`(구간 주석)는 해소 알림 처리(§1-1-1) 전까지 넣지 않습니다.
- **게이트에서 걸러진 사건은 안 찍습니다.** 그쪽이 다수라 타임라인이 우리가 진단한 노이즈와
  같은 모양이 됩니다.
- 주소는 전용 변수 `GATEWAY_GRAFANA_URL`(없으면 `GRAFANA_INTERNAL_URL`)이고, 기동 시
  `/api/health` 를 한 번 찍어 `/healthz` 에 참·거짓으로 노출합니다. 인증이 없는 경로라
  주소·경로·오류 문구는 내보내지 않습니다.
- 발행 실패는 주석 식별자를 비운 채 넘어갑니다. **발행 성공률을 상시 지표로 냅니다** —
  이것이 유일한 조용한 실패 탐지기입니다.

**§2-4-3 의 "Grafana 안에서" 요건은 절반만 충족합니다.** 소명(판단 근거 표시)은 주석으로
되지만, 대시보드 안의 버튼은 서명된 플러그인이 필요해 정정은 Keep 으로 돌렸습니다.
데이터 수집 목적은 달성, 위치 목적은 미달입니다.

### 25-5. 정확도 산출 (§2-4-1)

```
python -m gateway.quality --days 30
python -m gateway.quality --days 30 --send        # Zabbix trapper → Grafana
```

**라벨이 필요 없는 지표와 필요한 지표를 물리적으로 나눠 출력합니다.** 섞으면 읽는 사람이
전부 같은 신뢰도로 읽습니다.

| 구분 | 지표 |
|---|---|
| 관측(라벨 불요) | 게이트 발동률, 병합 규모 분포, 열화율, 응답시간 분위수, 주석 발행률, 소스 커버리지, 라우팅 분포, "동일" 응답 비율, 동시 발생 분할 건수 |
| 정확도(라벨 필요) | `overall` · `gate` · `merge` · `cause` |

- **표본이 모자라면 백분율 문자열을 만들지 않습니다.** 라벨이 `QUALITY_MIN_LABELS`(기본 20)
  미만이면 `판정 불가 (라벨 3/412, 최소 20)` 만 냅니다. 각주 붙은 92% 는 슬라이드에 92% 로
  옮겨집니다. 이는 브리지 마이닝의 "셔플 횟수 부족은 관계 없음이 아니라 판정 불가" 와 같은
  원칙입니다.
- 임계를 넘어도 **점추정 단독은 금지하고 Wilson 신뢰구간을 병기합니다.** 9/10 은 90% 가
  아니라 90%±폭입니다.
- **강제 재분석(`origin=forced`)은 분모에서 뺍니다.** 사람이 부른 것이라 게이트 발동률을
  부풀립니다. 한국어 사유 문자열로 거르면 문구 한 글자가 바뀔 때 조용히 깨지므로 컬럼으로
  구분합니다.
- **동시 발생 분할 건수**는 라벨 없이 병합을 재는 유일한 축입니다. 같은 호스트에서
  창 안에 서로 다른 지문으로 갈라져 열린 사건 수이며, 오류율이 아니라 **병합 후보**입니다.
  그대로 브리지 마이닝(§2-4-4)의 입력이 됩니다.
- **조치 성공률은 산출하지 않습니다.** 봇 판정 이력에 조치 결과가 회수되지 않습니다.
  Keep 워크플로 실행 이력 API 경유 가능성은 **미확인**입니다.
- `--send` 의 키는 `ansible/quality_metrics.yml` 의 아이템 키와 문자 단위로 같아야 합니다.
  어긋나면 전송은 성공(sender 가 실패로 안 봅니다)인데 화면만 조용히 빕니다. 셀프테스트가
  두 파일을 대조합니다.
- **산출 안 된 정확도는 `-1` 로 보냅니다.** `0` 은 "정확도 0%" 로 읽히고, 안 보내면 지난
  값이 화면에 그대로 남습니다(2026-07-31 월간 리포트에서 겪은 것과 같은 형태).

### 25-6. 과거 결론 연계 (§2-4-2)

만성·재발 사건에 과거 판정을 붙여 "이번에 달라진 것이 있는가" 를 묻습니다.

**조회 키는 3단이고 매칭 등급을 함께 싣습니다.**

| 순서 | 조건 | 등급 |
|---|---|---|
| 1 | 지문 일치 | 동일 사건 |
| 2 | `ikey`(영역\|호스트\|브리지) 일치 | 같은 유형 |
| 3 | 같은 영역·호스트 | 같은 호스트 |

지문만 보면 **재발일수록 못 찾습니다.** 같은 만성 문제라도 회차마다 병합 구성이 달라지면
지문이 갈리기 때문입니다. 등급을 안 실으면 모델이 같은 호스트의 무관한 옛 사건을
"이 사건의 과거 결론" 으로 읽습니다.

**주입 본체는 자유 문장이 아니라 구조화 필드입니다** — 판정, 유형, 심각도, 며칠 전인지,
매칭 등급, 사람 확인 여부. 여기엔 유출면이 없습니다.

**자유 문장은 조건을 전부 만족할 때만 붙습니다**(`GATEWAY_PRIOR_MODE=full` · 사람이 확인한
결론 · 이름 표가 비어 있지 않음 · 가린 뒤 아는 이름이 안 남음). 하나라도 어긋나면 문장을
버리고 구조화 필드만 보냅니다. 저장된 결론은 실명이고, 새 마스커가 아는 이름은 현재
컨텍스트와 전역 표뿐이라 지워진 호스트나 기동 직후의 빈 표에서는 그대로 나갑니다.

- **기본은 `structured`(본문 없음)** 입니다. 로드맵 원문이 "과거 **확정된 검증** 결론" 인데,
  사람이 안 본 LLM 출력은 확정도 검증도 아닙니다. 모든 항목에 "사람 확인됨" 또는
  "미확인(봇 출력)" 을 명시합니다.
- **오답으로 표시된 결론은 본문을 안 싣고 개수만 알립니다.** 모델은 "틀렸다" 고 적어 줘도
  그 문장에 기대어 같은 어휘로 새 가설을 씁니다.
- **반향 방지.** 주입된 결론을 모델이 되풀이하고 그것이 다시 저장되면, 세 번째 회차에는
  모델이 자기 문장을 읽고 "동일" 이라고 답합니다. 오염 안 된 원본(`prior_used=0`)을 먼저
  쓰고, 회신 마지막 줄에 `변화: 동일 | 달라짐 — ...` 를 요구해 `change` 로 저장합니다.
  **이 값이 전부 "동일" 로 수렴하는 것 자체가 반향 탐지기**이며 §25-5 가 상시 출력합니다.
- 주입은 `masking.build_llm_context` **안에서** 합니다. 그 함수가 화이트리스트 자체이고,
  전역 이름 표가 걸린 뒤에 마스킹해야 합니다.

### 25-7. 아직 아닌 것

- 조치 성공률 (위 25-5)
- 대시보드 안의 정정 버튼 (위 25-4)
- 구간 주석 — 해소 알림 처리에 종속
- `triage.run` 단건 경로는 이력을 안 남깁니다. 데모 스크립트 전용이므로 그대로 두고
  커버리지에 명시만 합니다.

---

## 26. 로그 선별 (Log Selection)

봇이 사건 하나에 실어 보내는 로그를 무엇으로 고르는지, 왜 그렇게 골랐는지를 적습니다.
전송 형태는 `docs/02-design/llm-data-contract.md` 가 정본이고, 측정과 개선 경위는
`private/docs/log_selection_20260813.md` 에 있습니다.

### 26-1. 접기가 본체이고 선별은 예비 경로입니다

랩 실측(2026-08-13)에서 **장애가 없는 평상시에도** 15분에 120줄을 쓰는 서버가 있었고,
그 120줄은 정규화 후 형태가 4종뿐이었습니다. 같은 모양을 3줄까지 접기만 해도 12줄이
됩니다. 그래서 **몫 선별은 평상시에 할 일이 없습니다.**

옛 코드는 전송 상한이 40줄이었기 때문에 몫 선별이 늘 도는 것처럼 보였지만, 실제로는
접기가 먼저 12줄로 줄여 놓아 몫은 그 12줄을 전부 가져갈 뿐이었습니다. **문서는 그 죽은
분기를 규칙인 것처럼 적고 있었습니다.** 지금은 순서를 코드에 명시합니다.

```
읽기(300줄/2MB)  →  접기(모양별 3줄 + 개수)  →  예산 안이면 그대로 전송
                                              →  넘으면 그때만 몫 선별
```

드물게 한 번 나타난 줄은 그 자체가 하나의 모양이므로 접기 단계에서 반드시 살아남습니다.
우리가 고치려던 고장(정상 260줄에 섞인 오류 3줄이 잘림)은 접기만으로 해결됩니다.

### 26-2. 예산을 줄 수가 아니라 글자 수로 잡은 이유

응답 시간 실측(2026-08-13, `claude-opus-4-8`, 각 3회 중앙값)입니다.

| 보낸 로그 | 입력 토큰 | 응답 시간 |
|---|---|---|
| 40줄 | 3,746 | 9.70초 |
| 400줄 | 37,136 | 11.18초 |
| 3,000줄 | 278,286 | 11.86초 |

입력을 75배 늘려도 응답 시간은 2.2초 늘어납니다. **40줄 상한이 지키던 것은 사실상
없었습니다.** 지연을 2초 아끼는 대가로 원인 줄을 버릴 위험을 계속 안고 있었습니다.

- `LOKI_SEND_BYTES`(64KB)가 실제 제동입니다. 줄 수는 줄 길이에 따라 같은 값이 열 배
  차이가 나므로 예산의 단위가 될 수 없습니다.
- `LOKI_SEND_LIMIT`(300줄)은 안전망이며 조회 상한과 같습니다.
- 64KB는 300줄이 평균 213자 이하일 때 통째로 나가는 크기입니다. 랩 실측 줄 길이는
  중앙값 108자·최대 233자입니다.

### 26-2b. 예비 경로의 몫 (접어도 예산을 넘을 때만)

| 몫 | 배정 | 대상 | 심각도 조건 |
|---|---|---|---|
| `error` | 14 | 오류로 판정된 줄 | 있음 |
| `novel` | 8 | 경고로 판정된 줄 | 있음 |
| `pre` | 12 | 첫 오류 **직전** 줄, 가까운 쪽부터 | 없음 |
| `recent` | 6 | 창의 마지막 줄부터 | 없음 |

**배정 값 14/8/12/6은 근거가 없는 임의값입니다.** 랩에서는 이 경로가 한 번도 돌지
않았으므로 정할 근거도 없습니다. 이 경로가 실제로 도는 환경을 만나면 그때 실측으로
정합니다.

**순서가 아니라 몫인 이유**: 오류부터 순서대로 채우면 재시도 루프 하나가 자리를 전부
먹고 원인을 담은 앞부분이 사라집니다.

**`pre` 를 가까운 쪽부터 보는 이유**: 앞에서부터 훑으면 오류에서 가장 먼 12줄이 실립니다.
원인은 대개 바로 앞에 있습니다(2026-08-13 감사에서 발견).

모델은 `why` 로 어느 경로였는지 압니다. `fold` 만 보이면 버려진 것은 반복된 줄뿐이고,
네 값이 보이면 그때부터 내용이 선별된 것입니다.

### 26-3. 심각도 판정

세 갈래로 봅니다. 앞의 둘은 형식이 분명해 대소문자를 안 가리고, 마지막은 평범한 문장과
구분이 안 되므로 **대문자일 때만** 인정합니다.

1. 등급 필드 — `level=` · `"severity":` · `"lvl":` · `"levelname":` · `"log.level":`
2. 괄호 안 — `[error]`(nginx) · `[core:error]`(Apache 2.4)
3. 문장 속 대문자 낱말 — `FATAL` · `ERROR` · `ERR` · `WARNING` · `WARN`

**`alert`·`crit`·`emerg` 는 3번 경로에서 뺐습니다.** 이 환경에서는 게이트웨이·Keep·Zabbix가
평상시에 쓰는 낱말이라, 그대로 두면 자기 로그가 오류 자리를 먹습니다
(`alert manager #1 started`, `cpu critical threshold = 90`).

등급 낱말이 없어도 중요한 줄은 따로 봅니다. 문구는 공식 소스로 대조했습니다.

| 문구 | 출처 |
|---|---|
| `Out of memory: Kill` | linux v5.14 `mm/oom_kill.c` |
| `Failed to start ` | systemd v252 `src/core/job.c` |
| `Failed with result '` | systemd v252 `src/core/unit.c` |
| `Dependency failed for `·`Timed out starting ` | systemd v252 `src/core/job.c` |
| `oom-kill`·`segfault` | 커널 메시지. 위 OOM 문구와 짝이거나 프로세스 비정상 종료 |
| `I/O error`·`EXT4-fs error` | 저장 장치·파일시스템 오류 |

**아래 두 줄은 공식 소스로 대조한 것이 아닙니다**(2026-08-19 정정). 표 위에 "공식 소스로
대조했다" 고만 적어 두어 네 줄 전부가 그런 것처럼 읽혔습니다. 특히 `I/O error` 는 일반
문장에도 나오는 말이라 오탐 여지가 있으니, 실환경에서 오탐이 관측되면 좁힐 후보입니다.

`entered failed state` 는 **뺐습니다.** systemd v219까지만 notice 등급이었고 지금은 debug라
Rocky 8·9 저널에 안 남습니다.

판정하지 못하면 미상이고, **미상을 오류로 올리지 않습니다.**

### 26-4. 같은 줄인지 판단하는 기준

변하는 값을 지운 비교용 형태를 만듭니다. **이 형태는 개수를 세기 위한 내부 값이며 절대
전송하지 않습니다.**

지우는 것: 시각(ISO·슬래시·RFC3164·Apache·맨 시각·날짜 단독), IP, UUID, 8자 이상 16진수,
`key=숫자`, 경로의 숫자 구간, 크기·소요시간, 그리고 **네 자리 이상 숫자**(프로세스 번호·
포트·바이트 수가 여기 들어갑니다. 세 자리 이하는 남깁니다 — 아래 표의 이유).

**지우지 않는 것**과 그 이유:

| 대상 | 이유 |
|---|---|
| HTTP 상태 코드 | `500`과 `200`이 같아지면 오류 줄과 정상 줄이 같은 형태가 되어 선별이 죽습니다 |
| 오류 번호·종료 코드·신호 번호·사용자 번호 | 서로 다른 사건이 한 형태로 합쳐집니다 |
| 경로의 디렉토리 이름 | `/etc/shadow`와 `/tmp/junk`가 합쳐지면 보안 관련 판단이 무너집니다 |

**보존할 숫자는 자리 수 규칙보다 먼저 감춥니다.** MySQL 오류 번호가 네 자리라 그냥 두면
`errno=1062`(중복 키)와 `errno=1236`(릴레이 로그 손상)이 같은 형태가 됩니다.

시각 형식을 하나 빠뜨리면 접기가 통째로 죽습니다. 실제로 슬래시 날짜 하나 때문에 한
서버에서 2,814줄이 1,503가지 형태로 세어졌고, 고친 뒤 4가지가 되었습니다.

### 26-5. 안 실린 구간

빠진 자리마다 `{"t":…, "gap": 223, "to":…}` 를 넣습니다. 고른 줄만 이어 붙이면 모델은
그것이 연속된 기록이라고 읽고 인접성에서 인과를 만듭니다. **줄 수와 시간 범위를 함께**
내는 이유는, 줄 수만으로는 그 사이에 몇 초가 비었는지 알 수 없기 때문입니다.

`실린 줄 수 + 생략 줄 수 == 읽어 온 줄 수` 를 무작위 입력 300회로 검사합니다.

### 26-6. 되돌리기

`LOG_SELECT_ENABLED=0` 이면 예전 동작(최신 N줄)으로 돌아갑니다. 커밋을 되돌리지 않고도
원래 상태와 비교할 수 있어야 합니다.

### 26-7. 이 기능에서 겪은 고장 (2026-08-13 감사)

멀티 에이전트 감사 두 건으로 열 건을 찾았습니다. 같은 실수를 되풀이하지 않도록 남깁니다.

- **상한이 무너졌다.** 값(시각+본문)으로 되찾다 보니 같은 줄이 같은 초에 겹치면 같은
  항목이 여러 번 붙어, 같은 줄 300개를 넣었을 때 300줄이 그대로 나갔습니다. 자리 번호로
  고르도록 바꿨습니다. 나노초를 초로 바꾸면 정밀도가 사라져 서로 다른 줄이 같은 시각이
  되는 경로가 실재합니다(120줄 중 114줄이 겹친 실측).
- **검사가 통과했다.** `<= 상한` 단언은 있었으나 시험 입력에 겹치는 줄이 없었습니다.
  사람이 상상한 입력만으로는 못 잡습니다. 무작위·극단 입력 불변 검사를 상설로 두었습니다.
- **생략 표시를 전송 줄로 셌다.** 41줄을 읽어 40줄을 보내면 표시 1개가 더해져 읽은 수와
  보낸 수가 같아지고, "일부만 실렸다"는 지시가 프롬프트에 안 붙었습니다.
- **선택 이유가 반복문 변수에 의존했다.** 몫 순서를 바꾸면 거짓 표시가 나갑니다.

## 27. 대화형 질의 (Ask)

사람이 관측 화면에서 자연어로 묻는 창구입니다. `ask.py`·`asktools.py`·`convo.py`·`app.py`
가 이 절을 가리킵니다. 절이 없어 후일담(§28~§32)만 흩어져 있었으므로 여기 모읍니다
(2026-08-19 신설).

### 27-1. 한 번의 질문이 지나는 길

1. **화면** — Grafana 앱 플러그인. 패널 메뉴에서 시작하면 대시보드 uid·패널 번호·호스트·
   조회 구간이 함께 넘어옵니다. 통신은 데이터소스 프록시를 거치므로 토큰이 브라우저에
   노출되지 않습니다.
2. **입구**(`app.ask_endpoint`) — 질의용 토큰 확인, 사용자별 시간당 상한, 대화 이력 적재.
3. **대상 표**(`ask.build_table`) — 허용된 감시 서버에서 호스트를 받아 `{토큰: {host,
   source, logs, security}}` 를 만듭니다. **이 표가 곧 경계입니다.** 표에 없으면 도구가
   대상을 지정할 방법이 없습니다.
4. **질문 위생**(`ask.sanitize_question`) — 제어문자 제거, 길이 제한, 가명화, 잔여 이름
   검사. 가릴 수 없으면 보내지 않습니다.
5. **반복문**(`graph.py` 또는 `ask.py` 의 직접 구현) — 모델이 도구를 고르고, 코드가
   실행하고, 결과를 다시 넣습니다. 조회는 여섯 번까지입니다.
6. **답**(`answer` 도구) — 산문 대신 구조를 받습니다. 결론·근거·본 구간·붙일 그림.
7. **회신** — 역치환해서 사람에게, 그림은 손잡이만 화면으로.

### 27-1-2. 파일 구조 (2026-08-19)

`ask.py` 한 파일이 1,289줄이라 정책·세션·조회기·답 조립·반복문이 같이 있었습니다. 열두
모듈로 나눴고 가장 큰 것이 364줄입니다. **밖에서는 예전처럼 `ask.run_ask` 로 씁니다** —
`__init__.py` 가 이름을 그대로 다시 내보냅니다.

```
gateway/ask/
  __init__.py   이름을 모아 두는 자리
  config.py     설정값과 고정 문구
  policy.py     닿을 수 있는 감시 영역
  table.py      조회 대상 표
  session.py    세션 역치환 표와 멈춤
  hygiene.py    질문 위생·이력 자르기
  answer.py     답 조립·시스템 문구
  budget.py     사용자별 사용량
  loop.py       질의 반복문
  engine.py     엔진 선택과 LangGraph 경로
  fetch/        loki · wazuh · zabbix · judgments · panels
```

**밖에서 갈아 끼우는 이름은 패키지가 기준입니다.** `build_table`·`fetch_problems`·
`FACTS_FILE`·`MAX_PER_USER_HOUR` 은 검사와 운영 도구가 `ask.X = ...` 로 바꿔 끼웁니다.
안쪽에서 파일 맨 위로 가져오면 그 교체가 안 보이므로 **부르는 시점에** 패키지에서 읽습니다
(`loop._pkg()`, `from . import FACTS_FILE as path`).

옮기면서 기능은 바꾸지 않았고, 구문 트리로 함수 본문을 대조해 확인했습니다. 이동 중에
셀프테스트가 세 가지를 잡았습니다 — 지식 조각 경로가 한 단계 얕아진 것, `lru_cache`
데코레이터가 빠진 것, 표준 라이브러리 들여오기 세 개가 빠진 것입니다.

### 27-2. 도구 아홉 개

| 도구 | 무엇을 하나 | 대상 |
|---|---|---|
| `list_hosts` | 물을 수 있는 호스트 목록 | 표에서 만든다(조회 없음) |
| `host_logs` | 그 호스트의 로그 | Loki |
| `security_alerts` | 보안 경보(레벨·규칙 그룹으로 좁힘) | Wazuh 인덱서 |
| `host_metrics` | 지표 추이(이름으로 고름) | Zabbix |
| `open_problems` | 지금 열려 있는 문제 | Zabbix |
| `past_judgments` | 봇이 전에 내린 판정 | 판정 이력 저장소 |
| `list_panels` | 볼 수 있는 관측 화면 목록 | Grafana |
| `panel_image` | 그 패널 그림 한 장 | Grafana 렌더러 |
| `answer` | 최종 답 | — |

**질의문은 전부 코드가 만듭니다.** 모델은 대상 토큰·기간·문자열 필터만 고릅니다. Zabbix 는
메서드 목록으로, Loki 와 Wazuh 는 "호출자가 질의문을 못 준다" 로 같은 경계를 만듭니다.

### 27-3. 지키는 것

- **대상**: 스키마 `enum` 이 표의 토큰으로 묶습니다. 목록 밖 이름은 표현할 수 없습니다.
- **읽기 전용**: `.get` 외 메서드 거부, LogQL·Wazuh 질의문 코드 조립.
- **이름**: 질문·이력·로그·패널 제목·질의 조건까지 이름 표를 거칩니다. 표가 비면 질의
  자체를 거부합니다(§B-2, `masking.cannot_mask`).
- **사람 구분**: 세션 열쇠에 신원을 넣습니다. 화면이 보내는 이름만으로는 안 나뉩니다(§32).
- **비용**: 사용자별 시간당 호출 상한, 라운드 상한, 결과 바이트 예산, 중복 조회 차단.

### 27-4. 잘렸으면 잘렸다고 말한다

이 창구에서 가장 위험한 고장은 조회 실패가 아니라 **성공처럼 보이는 실패**입니다. 그래서
모든 도구 결과에 실제로 본 구간(`window_utc`)을 싣고, 다음 경우에 안내를 붙입니다.

- 구간이 상한에 걸려 잘렸을 때
- 시각 인자를 못 읽어 무시했을 때
- 이름 검색이 상한에 걸려 일부만 실렸을 때(안 실은 이름도 함께)
- 로그가 줄 수 상한을 채웠을 때
- 지표 구간에 점이 하나도 없을 때
- 조회가 실패했거나 그 축이 없을 때(`status` 3종)

## 28. 질의 반복문 엔진 (LangGraph)

질의 창구의 반복문은 두 가지로 돌 수 있습니다. `ASK_ENGINE` 로 고릅니다.

| 값 | 무엇 | 비고 |
|---|---|---|
| `graph` | LangGraph (기본) | langgraph 가 없으면 자동으로 `loop` 로 돌아갑니다 |
| `loop` | 직접 구현한 반복문 | 되돌리는 길입니다 |

지금 무엇으로 도는지는 `/healthz` 의 `ask_engine` 과 질의 기록의 `engine=` 으로 봅니다.
설치가 빠지면 조용히 되돌아가므로 밖에서 보여야 합니다.

### 프레임워크가 맡는 것과 맡지 않는 것

**프레임워크가 맡는 것은 모델과 도구 사이를 오가는 흐름뿐입니다.** 나머지는 그대로
우리 것입니다.

- **모델 자리** — `graph.make_model` 이 만드는 껍데기가 `egress.call_raw` 를 부릅니다.
  `ChatAnthropic` 을 그대로 쓰면 프레임워크가 Anthropic 을 직접 부르므로 동시 수·시간당
  상한·사용자별 예산과 토큰 계수를 통째로 건너뜁니다.
- **도구 자리** — `ask.run_ask` 안의 `exec_tool` 을 두 엔진이 함께 씁니다. 중복 차단,
  그림 분리, 마스킹이 한 곳에 있어야 엔진을 갈아 끼울 때 한쪽만 빠지지 않습니다.
- **도구 정의** — 우리 `asktools.TOOL_SPECS` 를 그대로 보냅니다. 프레임워크 변환을 한 겹
  더 두면 사람이 읽고 고치는 정의와 모델이 받는 정의가 갈립니다.
- **상한과 멈춤** — 라운드·시간·바이트 상한과 사람이 누른 멈춤은 우리 분기(`route`)에서
  봅니다. 프레임워크 자체 상한(`recursion_limit`)에 먼저 닿으면 사람은 이유 대신 예외를
  봅니다. 그래서 프레임워크 상한은 넉넉히 두고 실제 제한은 우리가 겁니다.

### 파이썬 판 제약

`langgraph` 1.x 와 `langchain-core` 1.x 는 파이썬 3.10 이상을 요구하는데 랩과 실환경은
3.9 입니다. 3.9 를 받는 마지막 계열인 `langgraph==0.6.11` · `langchain-core==0.3.86` 으로
고정했고, 의존성 전체가 3.9 에 설치되는 것을 랩에서 확인했습니다(2026-08-18). 파이썬을
올리거나 컨테이너로 옮길 필요가 없습니다.

### 상태에 무엇을 담는가

`StateGraph` 는 **노드가 돌려주지 않은 열쇠를 유지하지 않습니다.** 상한과 멈춤 판단을
상태에 실었더니 첫 노드를 지나며 사라져 그래프가 통째로 실패했습니다(2026-08-18 랩 실측).
노드 사이를 오가는 값만 상태 열쇠로 선언하고, 함수와 상수는 클로저로 넘깁니다. 함수는
나중에 체크포인트를 켤 때 직렬화도 되지 않습니다.

### 통과 조건

- **셀프테스트** — 같은 가짜 모델을 주면 두 엔진의 답·추적·멈춘 이유가 같아야 합니다.
  프레임워크가 없는 곳에서는 건너뜁니다.
- **마스킹 누수 검사** — `bot/tools/leak_check_langgraph.py` 를 **실제로 올리는 조합**으로
  돌립니다. 모델이 받는 모든 문자열을 기록해 실명이 한 번이라도 나타나는지 봅니다.
- **랩 실측(2026-08-18)** — 같은 질문을 두 엔진에 던져 같은 도구를 부르고 같은 결론을
  내는 것을 확인했습니다.

### 왜 지금 옮겼는가, 그리고 무엇이 남았는가

옮겨서 지금 당장 얻는 기능은 없습니다. 얻는 것은 **다음 세 가지를 만들 때 직접 짜지
않아도 되는 자리**입니다.

1. 조사가 분기·병렬로 갈라질 때
2. 조사 도중 사람 승인을 받고 그 자리에서 이어갈 때(지금은 데모 B 를 Keep 승인 화면으로
   분리해 피하고 있습니다)
3. 조사 상태가 프로세스 재시작을 견뎌야 할 때(체크포인트)

기존 반복문은 되돌릴 길로 남겨 둡니다. 그래프가 실환경에서 한동안 문제없이 돈 뒤에
지웁니다.

## 29. 값 구속 · 캐싱 · 모델 등급

동기분 저장소(`Edrient17/aiops-rca-orchestrator`)의 LangGraph 사용 방식을 읽고 네 가지를
가져왔습니다. 폴더 재편은 하지 않았습니다.

### 스키마로 값을 묶는다

그쪽은 지금 존재하는 증거 식별자만 고를 수 있게 스키마를 그때그때 만듭니다. 프롬프트로
"지어내지 마라"라고 부탁하는 것과 값의 집합을 좁히는 것은 다릅니다. 부탁은 지켜질 때도
있고 아닐 때도 있지만, 스키마는 표현 자체를 막습니다.

우리는 `asktools.build_tool_specs(table)` 가 요청마다 정의를 만들고 호스트 토큰을 `enum`
으로 박습니다. 표 밖의 이름은 인자로 넣을 수 없습니다.

**묶는 자리를 고르는 기준은 캐시입니다.** 도구 정의는 프롬프트 맨 앞에 놓이므로 정의가
바뀌면 그 뒤 전부가 캐시에서 빠집니다. 그래서 요청 내내 안 바뀌는 값(호스트 토큰)만
`enum` 으로 묶고, 턴 중에 생기는 값(그림 손잡이·조회 구간)은 `check_answer` 가 코드로
검증합니다. 위반은 예외가 아니라 도구 결과로 돌려주어 모델이 스스로 고칩니다.

**정렬은 필수입니다.** 토큰 목록의 순서가 흔들리면 접두사 바이트가 달라져 캐시가 한 번도
안 걸립니다. 오류는 나지 않고 비용만 늘어납니다.

### 답도 도구로 받는다

`answer` 도구가 `summary`·`findings`·`window_utc`·`image_ids` 를 받습니다. 산문으로 받던
때는 그림 손잡이가 글자로 남고(`[img-6598]`), 조회하지 않은 구간이 지어내졌습니다.
답 도구를 안 부르고 글로 끝내는 경로는 그대로 둡니다.

### 프롬프트 캐싱

렌더 순서가 도구 → 시스템 → 대화라서 시스템 마지막 블록에 표시를 하나 달면 도구 정의와
시스템 문구가 함께 캐시됩니다. `LLM_CACHE=0` 으로 끕니다.

**모델마다 최소 길이가 다릅니다.** 접두사가 그 아래면 오류 없이 캐시가 멈추고 쓰기 값만
더 냅니다. haiku 4.5 는 최소 4,096 토큰입니다. 2026-08-19 랩 재실측입니다.

| | 입력 | 캐시 쓰기 | 캐시 읽기 |
|---|---|---|---|
| 1회차 | 326 | 8,803 | 0 |
| 2회차 | 326 | 0 | 8,803 |

접두사가 8월 18일 6,795 토큰에서 8,803 으로 늘었습니다. 도구가 하나 늘고 인자 설명이
길어진 만큼입니다. 프롬프트를 줄일 때 최소 길이까지 여유가 얼마인지 여기서 확인합니다.
같은 수치가 `llm.cached_system` 주석에도 있으니 함께 고칩니다.

### 모델 등급

`model_for(kind)` 가 용도별 환경변수를 보고, 없으면 `LLM_CLAUDE_MODEL` 로 떨어집니다.

**나누는 자리는 서로 다른 호출 사이뿐입니다.** 캐시는 모델별로 잡히므로 한 대화 안에서
모델을 바꾸면 도구·시스템·대화 캐시가 통째로 무효가 됩니다.

지금 실제로 내리는 것은 월간 리포트 서사 하나입니다. 트리아지와 질의 조사는 판단이라
큰 등급을 유지합니다. haiku 가 복제 지연 회신에 `RESET SLAVE` 를 권한 실측이 있습니다
(2026-08-13). `LLM_MODEL_ROUTE` 는 자리만 두었습니다. 새 호출을 만들면 비용이 오히려
늘어납니다.

### 상태 불변식

`graph.check_state` 가 조회 기록과 중복 차단 표의 개수, 그림 중복, 쓴 바이트, 멈춤 사유를
봅니다. 위반은 예외로 올리지 않고 `stopped="invalid_state"` 로 끝내 사람이 읽을 문장을
만듭니다. 분기 판단은 `should_continue` 로 그래프 밖에 두어 그래프 없이 단위 검사가
됩니다.

### 프롬프트 파일

`bot/prompts/ask.md`·`triage.md`·`monthly.md` 에서 읽습니다. 파일이 없으면 코드에 있는
상수로 돕니다. **실행 중에 다시 읽지 않습니다** — 프롬프트는 캐시 접두사의 일부라 도중에
바뀌면 그때까지 쌓인 캐시가 무효가 되고 같은 대화 안에서 앞뒤 문구가 달라집니다.

### 남은 한계 (2026-08-19 갱신)

**앞서 적었던 `at` 한계는 사라졌습니다.** 그 인자를 스키마에서 없앴기 때문에 모델이 고를 수
없습니다. 한때 남아 있던 코드 쪽 처리도 함께 정리했습니다. 해소된 것을 한계로 남겨 두면
다른 한계 서술의 신뢰도까지 떨어집니다.

지금 남은 것은 판단 품질입니다. haiku 는 조회 결과가 비었을 때 되부르지 않고 그대로 결론을
내는 경향이 있습니다. 그래서 되부르는 법을 결과에 적어 넣었고(§31-3), 그래도 남는 부분은
조사 등급을 큰 모델로 올려야 달라집니다. `LLM_MODEL_INVESTIGATE` 로 질의 경로만 올릴 수
있습니다.

## 30. 패널 그림이 7초 걸리는 이유

**질의 시간이 아니라 브라우저를 띄우는 고정 비용입니다.** 2026-08-18 랩 실측입니다.

| 패널 | 질의 | 시간 |
|---|---|---|
| 텍스트 (panelId=1) | 없음 | 6.91초 / 7.17초 |
| 시계열 CPU (Zabbix) | 있음 | 7.17초 / 7.24초 |
| 시계열 인증 (Wazuh) | 있음 | 7.23초 / 7.17초 |

질의가 하나도 없는 텍스트 패널이 같은 시간을 씁니다. 크기를 1000×320에서 600×200으로
줄여도, 렌더러를 `RENDERING_MODE: reusable` 로 바꿔도 값이 안 변했습니다.

패널 한 장에 5~15초는 이 도구에서 흔히 보고되는 범위이고([Improve Rendering
Times](https://github.com/grafana/grafana-image-renderer/issues/44)), 공식 문서에도 속도
설정은 `--browser.readiness.prior-wait`(기본 1초)·`--browser.scroll-wait`(기본 50밀리초)
정도뿐입니다([Troubleshoot image rendering](https://grafana.com/docs/grafana/latest/setup-grafana/image-rendering/troubleshooting/)).

**그래서 줄이지 않고 보이게 했습니다.** 그리는 동안 스피너와 "패널 로딩 중" 을 자리에
둡니다. 빈 칸이면 고장으로 보입니다. 실패하면 "패널을 못 불러왔습니다" 와 대시보드
링크가 남습니다.

답 자체는 그림을 기다리지 않습니다. 글이 먼저 오고 그림이 뒤따릅니다.

## 31. 화면이 준 구간과 상한에 닿았을 때의 마무리

### 31-1. 보던 구간을 모델에게 받아 적으라고 시키지 않습니다

패널에서 물으면 화면이 `from`·`to` 를 함께 넘깁니다(`grafana-app/src/pages/PageOne.tsx`
`panelFromQuery`). 그런데 도구는 모델이 그 값을 인자로 다시 넣어 주기를 기다렸고, 안
넣으면 조용히 기본 창(최근 1시간)으로 떨어졌습니다.

2026-08-18 랩 실측입니다. 8월 11일 19:18 ~ 13일 13:32 패널을 보고 물었더니 봇이 최근
1시간만 조회하고 "지정하신 과거 구간의 보안 기록 조회가 불가능합니다" 라고 답했습니다.
붙은 그림도 최근 1시간이었습니다. 패널 번호를 쥐고도 제목으로 대시보드를 뒤지던 것과
같은 모양입니다.

`asktools.window_bounds(args, now, max_m, default_span)` 이 화면 구간을 기본값으로
받습니다. 모델이 `from`·`to`·`window_m`·`at` 중 하나라도 주면 그쪽이 이깁니다. 사람이
"그럼 어제는?" 하고 물으면 그 요청이 화면 구간을 덮어야 합니다.

검사는 `run_ask → fetch_panel` 실경로로 렌더 주소의 `from`·`to` 를 봅니다. 함수만 직접
부르면 컨텍스트 배선이 빠져도 통과합니다.

### 31-2. 상한에 닿아도 답은 줍니다

조회 라운드를 다 쓰면 마지막에 남는 것이 모델의 중간 생각입니다. 실측된 회신은 "레벨을
더 낮춰서 전체 보안 이벤트를 확인하겠습니다." 한 줄이었습니다. 조회를 여덟 번 했는데 그
결과가 사람에게 하나도 가지 않았습니다.

상한·마감·예산에 닿았고 아직 답을 안 받았으면 `ask.force_answer` 가 한 번만 더 부릅니다.
그 호출에는 **`answer` 도구만 줍니다.** 조회 도구를 남기면 모델이 상한을 넘겨 또 조회하려
듭니다.

보내기 전에 결과가 안 붙은 도구 요청을 끝에서 걷어 냅니다(`ask.drop_dangling`). 멈춘
자리에 실행되지 않은 요청이 남아 있는데 그대로 보내면 Anthropic 이 400 으로 거부합니다
(`tool_use ids were found without tool_result blocks`). 그러면 마무리 호출까지 실패해
사람은 또 답을 못 받습니다.

마무리 호출도 실패하면 무엇을 조회했는지와 모델의 마지막 글을 함께 줍니다. 그래야
사람이 다시 물을 때 무엇이 빠졌는지 압니다.

### 31-3. 모델이 스스로 창을 넣었을 때

화면 구간을 기본값으로 넣어도 모델이 `window_m` 을 직접 적으면 그쪽이 이깁니다. 그
자체는 옳습니다. 사람이 같은 패널을 보면서 "그럼 지금은?" 하고 물을 수 있어야 합니다.

문제는 그다음이었습니다. 모델이 최근 창 결과를 받고 **화면 구간은 조회할 수 없다고
단정했습니다.** 실측된 회신입니다.

> 도구들이 현재 시간(2026-08-18 07:11 ~ 08:11)만 반환하고 있습니다. 지정하신 과거
> 구간은 조회 불가합니다.

그래서 본 구간이 화면 구간과 다르면 **결과에** 그 사실과 되부르는 법을 적습니다
(`asktools.span_note`). 지시문에만 적는 것으로는 부족했습니다. 같은 질문 다섯 번을
Grafana 프록시 경유로 다시 보내 다섯 번 모두 요청한 구간으로 답하는 것을 확인했습니다.

`app.py` 는 화면이 넘긴 패널 값을 로그에 남깁니다. 회신만 보면 구간이 아예 안 온
것인지 게이트웨이가 받고도 안 쓴 것인지 가릴 수 없었습니다.

### 31-4. 로그 상한을 보안 경보와 맞췄습니다

이틀 구간을 보며 물었더니 보안 경보는 전 구간을 보는데 로그만 하루로 잘렸습니다.
봇이 "2026-08-11 19:18부터 12일 13:32 사이는 조회 제한" 이라고 답했습니다. 절반만 보고
답한 셈입니다.

로그도 7일 상한을 씁니다. 결과 크기는 구간이 아니라 줄 수 상한이 정하므로 구간을
넓혀도 무겁지 않습니다. 대신 줄 수 상한을 채우면 최신 쪽만 실렸다고 결과에 적습니다
(`asktools.note_if_capped`). 로그는 최신순으로 받으므로, 안 알리면 실린 것이 그 구간
전부인 줄 알고 "없었다" 로 답합니다.

**정정 (2026-08-19).** 위 통지는 붙는 조건이 잘못돼 있어 **한 번도 발화하지 않았습니다.**
질의는 늘 60줄로 조회하는데 통지는 알림 경로의 상수(300줄)와 견주고 있었습니다. 고쳤다고
적어 둔 고장이 임계값 불일치로 살아 있던 셈입니다. 지금은 그 조회에 실제로 쓴 상한과
견주고, 모델이 `limit` 으로 300줄까지 더 받을 수 있습니다. 검사도 조립 함수 직접 호출이
아니라 도구를 실제로 돌려서 봅니다 — 그 공백이 이 고장을 낳았습니다.

### 31-5. 패널은 제목이 아니라 번호로 가리킵니다

제목으로 찾는 한 이름이 비슷한 옆 패널이 걸립니다. 2026-08-18 하루에 세 번 겪었습니다.
보고 있던 패널 대신 다른 대시보드의 패널을 붙였고, 다른 대시보드를 물었을 때는 보고 있던
대시보드에서 찾아 놓고 "그 대시보드에는 없습니다" 라고 답했으며, 패널 목록을 돌려주자
목록에서 고르지 않고 같은 조회를 세 번 반복했습니다.

그래서 제목을 넘기는 인자를 없앴습니다. `list_panels` 가
`[{ref, dashboard, title, source, query}]` 를 돌려주고, `panel_image` 는 그 `ref` 만
받습니다. `source` 와 `query` 는 그 패널이 무엇을 조회하는지입니다 — 제목만 주면 두 패널이
같은 값을 보는지 짐작하게 되고, 짐작은 틀립니다. 그림 손잡이(`img-...`)와 같은 방식이라
모델은 `pnl-13` 만 보고 대시보드 식별자와 패널 번호는 서버가 들고 있습니다. 모르는
손잡이는 거부합니다.

랩 실측입니다. "MSP 월간 리포트의 인증 패널도 같은 값이냐" 에 `list_panels` →
`pnl-13` → `panel_image` 로 `kinx-msp-report` 의 16번 패널이 같은 호스트·같은 구간으로
그려졌고, 답도 두 패널을 실제로 비교한 내용이었습니다.

**엄격 모드 도구 정의는 선택 인자를 24개까지만 갖습니다.** 25개가 되자 모든 질의가 400
으로 죽었습니다("Schemas contains too many optional parameters"). `match` 와 `dashboard`
를 빼고 `panel_ref` 와 목록 도구의 `dashboard` 를 넣어 정확히 24개로 맞췄습니다.
셀프테스트가 이 수를 세므로 다음에 인자를 더할 때 랩이 아니라 검사에서 걸립니다.

## 32. 운영 배치 (2026-08-19)

### 32-1. 기동은 systemd 가 맡습니다

`restart_gateway.sh` 가 nohup 으로 띄우던 것을 유닛으로 옮겼습니다. 유닛 파일은
`bot/deploy/kinx-gateway.service`, 설치는 `ansible-playbook gateway_service.yml` 입니다.
스크립트는 유닛이 등록돼 있으면 그쪽으로 재기동합니다. 두 방식이 같이 돌면 같은 포트를
다투고, 죽었을 때 살아나는 쪽과 사람이 보는 쪽이 달라집니다.

워커는 하나로 둡니다. 워커마다 인시던트 버퍼와 세션 표가 따로 생겨 부모 카드가 여러 개
뜹니다. 다중화하려면 그 두 상태를 먼저 Redis 로 옮겨야 합니다.

로그는 저널로 보냅니다. `/tmp/gw-verify.log` 는 회전이 없어 무한정 쌓였고, 실 호스트명이
남는 파일인데 같은 기계의 다른 사용자가 읽을 수 있었습니다. 저널은 보존 14일·500MB 로
제한합니다. 호스트명 자체는 계속 남깁니다 — 어느 기계의 사건인지 모르면 운영 로그의
쓸모가 없고, 이제 읽을 수 있는 사람이 제한됩니다.

```bash
journalctl -u kinx-gateway -f
```

### 32-2. 이름 표가 얼어붙는 것을 봅니다

갱신 스레드는 예외를 삼키고 직전 표를 계속 씁니다. 조회 토큰이 만료되면 표가 그 시점으로
굳고 로그에만 오류가 쌓입니다. 그 사이 온보딩한 호스트는 표에 없고, 질의 경로가 그 이름을
원문으로 내보냅니다. **아무 지표도 안 움직여 발견할 계기가 없었습니다.**

생존 신호에 세 값을 더했습니다 — `gateway.names`(항목 수), `gateway.names_age`(갱신 경과
초, 한 번도 못 만들었으면 -1), `gateway.names_error`(갱신 오류 여부). `/healthz` 에도
`names` 와 `names_error` 를 싣습니다.

트리거는 둘입니다. 3시간 넘게 안 갱신되거나 오류가 있으면 하나, 하루 최대치보다 30% 이상
줄면 다른 하나입니다. **줄어드는 쪽이 중요합니다.** 0 이 되면 질의가 아예 막히지만(§B-2),
일부 감시 서버만 빠지면 화면에는 아무 이상이 없습니다.

### 32-3. 토큰을 용도별로 나눴습니다

랩 실측입니다. Viewer 권한 계정이 Grafana 데이터소스 프록시로 `/v1/messages` 를 부르니
회사 키로 실제 답이 왔습니다(200). 프록시가 선언한 경로의 하위 경로를 전부 통과시키는데
게이트웨이 토큰이 하나뿐이라 용도를 가릴 수 없었습니다.

`ASK_TOKEN` 을 두고 `/ask` 계열만 받게 했습니다. 웹훅과 LLM 중계는 이 토큰을 거부합니다.
안 적은 배포는 예전처럼 하나로 돕니다.

| 경로 | 게이트웨이 토큰 | 질의 토큰 |
|---|---|---|
| `/ask`, `/ask/convos*`, `/ask/cancel` | 통과 | 통과 |
| `/webhook/zabbix`, `/webhook/wazuh` | 통과 | **거부** |
| `/v1/messages` | 통과 | **거부** |

분리 뒤 같은 계정으로 다시 확인했습니다. 질의 200, 웹훅 인증 거부, 중계 인증 거부입니다.

**한 가지 정정.** 점검 문서는 웹훅 응답 422 를 "핸들러까지 닿았다" 로 읽었는데, 422 는
FastAPI 가 본문을 검증하다 낸 것이라 인증 이전 단계입니다. 실제로 인증을 통과했던 것은
`/v1/messages` 쪽이며, 그것은 직접 확인했습니다.

### 32-4. 느린 저장소가 서버 전체를 세우지 않습니다

`/ask` 는 비동기인데 그 안에서 Redis 와 SQLite 를 그대로 불렀습니다. Redis 클라이언트는
동기이고 시한이 2초라, 저장소가 늦으면 호출마다 이벤트 루프가 서고 한 요청에 세 번이므로
6초입니다. 그동안 웹훅 수신도 다른 질의도 인시던트 타이머 마감도 함께 멈춥니다.

`app._off_loop` 로 감싸 워커 스레드에서 돌립니다. 나머지 대화 관리 엔드포인트는 `def`
라서 FastAPI 가 이미 워커에서 돌리므로 그대로 둡니다.

### 32-5. 질의 성능 (2026-08-19 실측)

| 무엇 | 전 | 후 |
|---|---|---|
| 지표 8개 조회 | 아이템마다 한 번씩 최대 10왕복 | 값 유형으로 묶어 3왕복 |
| 같은 조회 소요 | (콜당 5초 × 왕복) | 0.15초 (랩, 아이템 8개·표본 60점) |
| 관측 화면 목록 | 대시보드 상세를 순차로 최대 50회 | 병렬 6줄기·상한 20개 |
| 대상 표 | 질문마다 감시 서버 전체 재조회 | 60초 캐시 (실패는 캐시 안 함) |
| 도구 한 번 | 시한 없음(최악 분 단위) | 20초, 넘으면 `unavailable` |

질의 한 건이 조회 1회에 5초입니다(랩, haiku). 왕복 수는 저널에서 셉니다.

```bash
journalctl -u kinx-gateway --since -2min | grep -c api_jsonrpc.php
```

**캐시를 길게 잡지 않는 이유**가 있습니다. 이름 표는 갱신이 실패하면 직전 값을 유지하지만,
대상 표는 그러면 안 됩니다. 그 표가 낡으면 봇이 **없는 호스트를 있다고 답합니다.** 그래서
60초이고, 조회가 실패하면 아예 캐시하지 않습니다.

## 33. 질의 추적 (LangSmith)

질의 한 건이 무엇을 얼마나 쓰는지 화면으로 보기 위한 것입니다. **켜면 외부로 값이 한 벌 더
나가므로** 이 저장소의 다른 반출 지점과 같은 규칙을 적용합니다.

### 33-1. 무엇이 나가고 무엇이 안 나가나

나가는 것은 그래프 질의 경로의 노드 입출력입니다. 시스템 문구, 도구 정의, 도구 결과, 모델
회신이며 **전부 이미 가명 처리된 값**입니다. 마스킹은 도구 결과를 조립할 때 끝나므로
추적에 실리는 것도 `[host-1]` 형태입니다. 역치환 표는 프로세스 메모리에만 있어 나가지
않습니다.

**안 잡히는 것이 있습니다.** 프레임워크를 지나는 것은 그래프 질의 경로뿐입니다. 트리아지·
월간 리포트·`ASK_ENGINE=loop` 은 우리 출구에서 SDK 를 직접 부르므로 추적에 안 나타납니다.
화면에 보이는 것이 전부라고 읽으면 비용을 적게 셉니다. 전체 사용량은 판정 이력 저장소의
`usage` 표(호출별 입력·출력·캐시 읽기/쓰기·모델)가 여전히 기준입니다.

### 33-2. 켜는 법

`bot/.env` 에 두 값을 적습니다. **하나만 적으면 켜지지 않습니다** — 켜진 척하다가 아무 데도
안 보내는 상태를 만들지 않기 위해서입니다.

```
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=<발급받은 키>
LANGSMITH_PROJECT=kinx-gateway
```

기동 로그와 `/healthz` 의 `tracing` 으로 상태를 확인합니다. 끄려면 `LANGSMITH_TRACING` 을
지우고 재기동합니다. 라이브러리가 없어도 질의는 그대로 돕니다(선택 의존).

### 33-3. 켜기 전에 한 번 볼 것

**첫 추적 한 건을 열어 실명·사설 IP·토큰이 있는지 봅니다.** 하나라도 보이면 켜 두지 말고
원인을 먼저 고칩니다. 지금까지 이 저장소에서 이름이 샌 경로는 전부 "가리는 자리를 한 곳
빠뜨린" 형태였고(2026-08-19 감사 B-1), 추적은 그 빠뜨림을 그대로 복사합니다.
