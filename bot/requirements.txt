# 하한만 지정 — VM 실측 파이썬 3.9 호환 (dev는 3.14였음). pip이 런타임에 맞는 최신을 고름.
anthropic>=0.120.0
fastapi>=0.110.0
uvicorn>=0.27.0
httpx>=0.25.0
# 호스트 명부(hosts.yml) 파서. 없으면 명부 기능만 비활성되고 나머지는 돈다.
pyyaml>=6.0

# 질의 반복문을 LangGraph 로 돌릴 때만 필요하다(ASK_ENGINE=graph). 1.x 계열은 파이썬
# 3.10 이상을 요구하는데 랩과 실환경이 3.9 라 0.6 계열로 고정한다. 2026-08-18 랩에서
# 의존성 전체가 3.9 에 설치되는 것을 확인했다.
langgraph==0.6.11
langchain-core==0.3.86

# 심층 조사의 축약 전용(GPT-5.6 Luna). 없으면 축약이 haiku 로 내려가고 나머지는 그대로
# 돈다 — 어댑터가 지연 임포트라 미설치가 기동을 막지 않는다. 판단 경로에는 안 들어간다.
openai>=1.40.0
